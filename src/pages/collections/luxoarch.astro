---
/**
 * collections/luxoarch.astro
 *
 * luxoⒶRCH Collection Page — v2.4.0
 * Outdoor lighting collection: 23 families, 269 SKUs, 8 applications.
 *
 * Uses BaseLayout (canonical Header + Footer via shared partials).
 * All Ⓐ via <BrandMark> component.
 * Page-specific styles inline in <style> block, scoped.
 */
import BaseLayout from '../../layouts/BaseLayout.astro';
import BrandMark from '../../components/BrandMark.astro';

/* Fix 3 (v2.4.2): canonical application tokens added to each family.
 * Tokens match checkbox values exactly (kebab-case). Liberty has two tokens. */
const families = [
  { sub: 'SPORTS LIGHTING', tier: 'PRO',  name: 'Illuminator',   maxW: '800W',  skus: 11, dlc: true,  apps: 'sports-lighting' },
  { sub: 'AREA LIGHT',      tier: 'PRO',  name: 'Liberty',       maxW: '450W',  skus: 25, dlc: true,  apps: 'area-light wall-pack' },
  { sub: 'SPORTS LIGHTING', tier: 'PRO',  name: 'Radiator-II',   maxW: '1200W', skus: 10, dlc: true,  apps: 'sports-lighting' },
  { sub: 'STRING LIGHT',    tier: 'ECO',  name: 'LSL Series',    maxW: 'n/a',   skus: 20, dlc: false, apps: 'string-light' },
  { sub: 'SPORTS LIGHTING', tier: 'PRO+', name: 'Beacon-I',      maxW: '1200W', skus: 3,  dlc: true,  apps: 'sports-lighting' },
  { sub: 'FLOOD LIGHT',     tier: 'PRO+', name: 'Zion-II',       maxW: '300W',  skus: 16, dlc: true,  apps: 'flood-light' },
  { sub: 'WALL PACK · ADJUSTABLE', tier: 'ECO', name: 'Anaheim', maxW: '80W',   skus: 10, dlc: true,  apps: 'wall-pack' },
  { sub: 'WALL PACK · FULL CUTOFF', tier: 'PRO', name: 'Nightwatch', maxW: '100W', skus: 10, dlc: true, apps: 'wall-pack' },
  { sub: 'AREA LIGHT',      tier: 'ECO',  name: 'Heritage',      maxW: '300W',  skus: 4,  dlc: true,  apps: 'area-light' },
  { sub: 'AREA LIGHT',      tier: 'PRO+', name: 'Rosetta-III',   maxW: '300W',  skus: 8,  dlc: true,  apps: 'area-light' },
  { sub: 'FLOOD LIGHT',     tier: 'ECO',  name: 'Everest',       maxW: '150W',  skus: 8,  dlc: true,  apps: 'flood-light' },
  { sub: 'WALL PACK · CLASSIC', tier: 'ECO', name: 'Guardian',   maxW: '120W',  skus: 4,  dlc: true,  apps: 'wall-pack' },
  { sub: 'WALL PACK · CLASSIC', tier: 'PRO', name: 'Sentinel',   maxW: '120W',  skus: 5,  dlc: true,  apps: 'wall-pack' },
  { sub: 'WALL PACK · FULL CUTOFF', tier: 'ECO', name: 'Watchtower', maxW: '120W', skus: 5, dlc: true, apps: 'wall-pack' },
  { sub: 'CANOPY',          tier: 'PRO',  name: 'Pathfinder',    maxW: '110W',  skus: 4,  dlc: true,  apps: 'canopy' },
  { sub: 'CYLINDER',        tier: 'PRO',  name: 'Aura',          maxW: '45W',   skus: 4,  dlc: false, apps: 'cylinder' },
  { sub: 'SPORTS LIGHTING', tier: 'PRO',  name: 'Radiator-I',    maxW: '1200W', skus: 2,  dlc: true,  apps: 'sports-lighting' },
  { sub: 'WALL PACK · ARCHITECTURAL SLIM', tier: 'ECO', name: 'Shield', maxW: '120W', skus: 4, dlc: true, apps: 'wall-pack' },
  { sub: 'DOCK LIGHT',      tier: 'PRO',  name: 'Canadarm',      maxW: '60W',   skus: 2,  dlc: false, apps: 'dock-light' },
  { sub: 'WALL PACK · ENTRYWAY', tier: 'ECO', name: 'Wedge-I',   maxW: '20W',   skus: 4,  dlc: true,  apps: 'wall-pack' },
  { sub: 'WALL PACK · ADJUSTABLE', tier: 'PRO', name: 'Atlanta', maxW: '120W',  skus: 2,  dlc: true,  apps: 'wall-pack' },
  { sub: 'WALL PACK · ENTRYWAY', tier: 'ECO', name: 'Ramparts',  maxW: '20W',   skus: 2,  dlc: true,  apps: 'wall-pack' },
  { sub: 'SPORTS LIGHTING', tier: 'PRO',  name: 'Radiator-III',  maxW: '480W',  skus: 1,  dlc: true,  apps: 'sports-lighting' },
];

const tierColor: Record<string, string> = { 'PRO': '#F32740', 'PRO+': '#FFC107', 'ECO': '#5B8DEF' };
---
<BaseLayout
  title="luxoARCH Outdoor Collection — Archipelago Lighting Group"
  description="Outdoor lighting for the commercial perimeter. 23 active families, 269 active SKUs, ECO through PRO+."
  bodyClass="page-luxoarch"
>
  <!-- ================================================================
       BREADCRUMB BAND
       ================================================================ -->
  <div class="breadcrumb-band">
    <a href="/">Home</a>
    <span class="bc-sep">/</span>
    <a href="#">Products</a>
    <span class="bc-sep">/</span>
    <a href="#">Outdoor</a>
    <span class="bc-sep">/</span>
    <span class="bc-current"><BrandMark prefix="luxo" suffix="RCH" /></span>
  </div>

  <!-- ================================================================
       §2 HERO — dark band + gradient placeholder image strip
       ================================================================ -->
  <section class="hero-band" id="sec-hero">
    <div class="hero-top">
      <div class="hero-eyebrow">OUTDOOR · COLLECTION</div>
      <h1 class="hero-wordmark">LUXO<BrandMark prefix="" suffix="RCH" />
      </h1>
      <p class="hero-desc">Outdoor lighting for the commercial perimeter. 23 active families, 269 active SKUs, ECO through PRO+.</p>
      <div class="hero-pills">
        <span class="hero-pill">DLC PREMIUM</span>
        <span class="hero-pill">TAA AVAILABLE</span>
        <span class="hero-pill">5 US WAREHOUSES</span>
        <span class="hero-pill">3-DAY DELIVERY</span>
        <span class="hero-pill">8 APPLICATIONS</span>
      </div>
    </div>
    <!-- Fix 3 (v2.4.1): hero-image-strip removed; re-add in v2.4.x with hero-luxo.jpg -->
  </section>

  <!-- ================================================================
       §3 STAT STRIP — 4 columns on dark
       ================================================================ -->
  <section class="stat-strip" id="sec-stats">
    <div class="stat-col">
      <div class="stat-num">23</div>
      <div class="stat-label">Fixture Families</div>
    </div>
    <div class="stat-divider"></div>
    <div class="stat-col">
      <div class="stat-num">269</div>
      <div class="stat-label">Active SKUs</div>
    </div>
    <div class="stat-divider"></div>
    <div class="stat-col">
      <div class="stat-num">8</div>
      <div class="stat-label">Applications</div>
    </div>
    <div class="stat-divider"></div>
    <div class="stat-col">
      <div class="stat-num stat-red">DLC</div>
      <div class="stat-label">Premium Listed</div>
    </div>
  </section>

  <!-- ================================================================
       §4 BROWSE BY APPLICATION — 8 SVG-icon tiles
       ================================================================ -->
  <section class="page-section" id="sec-apps">
    <div class="section-inner">
      <div class="sec-eyebrow">Browse by application</div>
      <div class="app-grid">

        <!-- Sports Lighting -->
        <div class="app-tile" data-app="Sports Lighting">
          <svg class="app-icon" width="28" height="28" viewBox="0 0 28 28" fill="none" xmlns="http://www.w3.org/2000/svg">
            <circle cx="14" cy="14" r="10" stroke="#8a8f99" stroke-width="1.5"/>
            <line x1="14" y1="4" x2="14" y2="24" stroke="#8a8f99" stroke-width="1.2"/>
            <line x1="4" y1="14" x2="24" y2="14" stroke="#8a8f99" stroke-width="1.2"/>
            <circle cx="14" cy="14" r="3" stroke="#8a8f99" stroke-width="1.2"/>
          </svg>
          <div class="app-name">Sports Lighting</div>
          <div class="app-sku">61 SKUs</div>
        </div>
        <!-- Area Light -->
        <div class="app-tile" data-app="Area Light">
          <!-- Fix 4 (v2.4.2): parking-lot pole with arm + fixture -->
          <svg class="app-icon" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#8a8f99" stroke-width="1.5" xmlns="http://www.w3.org/2000/svg">
            <line x1="12" y1="3" x2="12" y2="18"/>
            <line x1="12" y1="5" x2="19" y2="5"/>
            <rect x="17" y="5" width="4" height="3"/>
          </svg>
          <div class="app-name">Area Light</div>
          <div class="app-sku">90 SKUs</div>
        </div>

        <!-- Wall Pack -->
        <div class="app-tile" data-app="Wall Pack">
          <!-- Fix 4 (v2.4.2): vertical wall edge + protruding fixture -->
          <svg class="app-icon" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#8a8f99" stroke-width="1.5" xmlns="http://www.w3.org/2000/svg">
            <line x1="6" y1="3" x2="6" y2="21"/>
            <rect x="6" y="9" width="12" height="6"/>
            <line x1="9" y1="15" x2="9" y2="18"/>
            <line x1="15" y1="15" x2="15" y2="18"/>
          </svg>
          <div class="app-name">Wall Pack</div>
          <div class="app-sku">52 SKUs</div>
        </div>

        <!-- Flood Light -->
        <div class="app-tile" data-app="Flood Light">
          <!-- Fix 4 (v2.4.2): rect fixture at top + inverted triangle beam -->
          <svg class="app-icon" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#8a8f99" stroke-width="1.5" xmlns="http://www.w3.org/2000/svg">
            <rect x="9" y="3" width="6" height="3"/>
            <path d="M 6 18 L 12 6 L 18 18 Z"/>
          </svg>
          <div class="app-name">Flood Light</div>
          <div class="app-sku">24 SKUs</div>
        </div>

        <!-- String Light -->
        <div class="app-tile" data-app="String Light">
          <svg class="app-icon" width="28" height="28" viewBox="0 0 28 28" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M4 8 Q7 4 10 8 Q13 12 16 8 Q19 4 22 8 Q25 12 28 8" stroke="#8a8f99" stroke-width="1.5" fill="none"/>
            <circle cx="7" cy="8" r="2" stroke="#8a8f99" stroke-width="1.2"/>
            <circle cx="14" cy="10" r="2" stroke="#8a8f99" stroke-width="1.2"/>
            <circle cx="21" cy="8" r="2" stroke="#8a8f99" stroke-width="1.2"/>
          </svg>
          <div class="app-name">String Light</div>
          <div class="app-sku">20 SKUs</div>
        </div>

        <!-- Canopy -->
        <div class="app-tile" data-app="Canopy">
          <!-- Fix 4 (v2.4.2): inverted-U dome + hanging luminaire -->
          <svg class="app-icon" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#8a8f99" stroke-width="1.5" xmlns="http://www.w3.org/2000/svg">
            <path d="M 3 8 Q 12 3 21 8"/>
            <line x1="3" y1="8" x2="3" y2="11"/>
            <line x1="21" y1="8" x2="21" y2="11"/>
            <circle cx="12" cy="14" r="2"/>
          </svg>
          <div class="app-name">Canopy</div>
          <div class="app-sku">7 SKUs</div>
        </div>

        <!-- Cylinder -->
        <div class="app-tile" data-app="Cylinder">
          <!-- Fix 4 (v2.4.2): vertical pill/capsule + downlight beam -->
          <svg class="app-icon" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#8a8f99" stroke-width="1.5" xmlns="http://www.w3.org/2000/svg">
            <rect x="9" y="3" width="6" height="14" rx="3"/>
            <line x1="10" y1="20" x2="14" y2="20"/>
          </svg>
          <div class="app-name">Cylinder</div>
          <div class="app-sku">4 SKUs</div>
        </div>

        <!-- Dock Light -->
        <div class="app-tile" data-app="Dock Light">
          <!-- Fix 4 (v2.4.2): lamp head + articulating arm -->
          <svg class="app-icon" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#8a8f99" stroke-width="1.5" xmlns="http://www.w3.org/2000/svg">
            <circle cx="18" cy="8" r="3"/>
            <line x1="15" y1="10" x2="6" y2="20"/>
            <circle cx="6" cy="20" r="1.5"/>
          </svg>
          <div class="app-name">Dock Light</div>
          <div class="app-sku">4 SKUs</div>
        </div>

      </div>
    </div>
  </section>

  <!-- ================================================================
       §5 RED STAT BANNER — 6 columns on brand red
       ================================================================ -->
  <section class="red-banner" id="sec-red-banner">
    <div class="red-banner-inner">
      <div class="rb-col"><div class="rb-num">23</div><div class="rb-label">Families</div></div>
      <div class="rb-div"></div>
      <div class="rb-col"><div class="rb-num">269</div><div class="rb-label">Active SKUs</div></div>
      <div class="rb-div"></div>
      <div class="rb-col"><div class="rb-num">8</div><div class="rb-label">Applications</div></div>
      <div class="rb-div"></div>
      <div class="rb-col"><div class="rb-num">DLC</div><div class="rb-label">Premium</div></div>
      <div class="rb-div"></div>
      <div class="rb-col"><div class="rb-num">TAA</div><div class="rb-label">Available</div></div>
      <div class="rb-div"></div>
      <div class="rb-col"><div class="rb-num">5 US</div><div class="rb-label">Warehouses</div></div>
    </div>
  </section>

  <!-- ================================================================
       §6 FEATURED FAMILIES — 5 dark cards
       ================================================================ -->
  <section class="page-section" id="sec-featured">
    <div class="section-inner">
      <div class="sec-eyebrow">Featured families</div>
      <h2 class="sec-title">Five flagship families for the commercial perimeter.</h2>
      <p class="sec-sub">The most-specified <BrandMark prefix="luxo" suffix="RCH" /> families across stadium, parking, wall, and flood applications.</p>
      <div class="featured-grid">

        <!-- Card 1: Aura — grey block placeholder (Fix 1 v2.4.2) -->
        <div class="fam-card">
          <div class="fam-card-top">
            <span class="fam-sub-tag">CYLINDER</span>
            <span class="tier-badge tier-pro">PRO</span>
          </div>
          <div class="fam-img-placeholder">Aura</div>
          <div class="fam-card-bottom">
            <div class="fam-info">
              <div class="fam-name">Aura</div>
              <div class="fam-meta">45W max · 4 SKUs</div>
              <span class="dlc-pill">DLC</span>
            </div>
            <a href="#" class="fam-view">VIEW →</a>
          </div>
        </div>

        <!-- Card 2: Illuminator — line drawing (Fix 1 v2.4.2) -->
        <div class="fam-card">
          <div class="fam-card-top">
            <span class="fam-sub-tag">SPORTS LIGHTING</span>
            <span class="tier-badge tier-pro">PRO</span>
          </div>
          <div class="fam-img-wrap">
            <img src="/images/collections/luxoarch/line-drawings/line-illuminator.png" alt="Illuminator line drawing" class="fam-line-img" />
          </div>
          <div class="fam-card-bottom">
            <div class="fam-info">
              <div class="fam-name">Illuminator</div>
              <div class="fam-meta">800W max · 11 SKUs</div>
              <span class="dlc-pill">DLC</span>
            </div>
            <a href="#" class="fam-view">VIEW →</a>
          </div>
        </div>

        <!-- Card 3: Liberty — line drawing (Fix 1 v2.4.2, SKU 24→25) -->
        <div class="fam-card">
          <div class="fam-card-top">
            <span class="fam-sub-tag">AREA LIGHT</span>
            <span class="tier-badge tier-pro">PRO</span>
          </div>
          <div class="fam-img-wrap">
            <img src="/images/collections/luxoarch/line-drawings/line-liberty.png" alt="Liberty line drawing" class="fam-line-img" />
          </div>
          <div class="fam-card-bottom">
            <div class="fam-info">
              <div class="fam-name">Liberty</div>
              <div class="fam-meta">450W max · 25 SKUs</div>
              <span class="dlc-pill">DLC</span>
            </div>
            <a href="#" class="fam-view">VIEW →</a>
          </div>
        </div>

        <!-- Card 4: Nightwatch — grey block placeholder (Fix 1 v2.4.2) -->
        <div class="fam-card">
          <div class="fam-card-top">
            <span class="fam-sub-tag">WALL PACK · FULL CUTOFF</span>
            <span class="tier-badge tier-pro">PRO</span>
          </div>
          <div class="fam-img-placeholder">Nightwatch</div>
          <div class="fam-card-bottom">
            <div class="fam-info">
              <div class="fam-name">Nightwatch</div>
              <div class="fam-meta">100W max · 10 SKUs</div>
              <span class="dlc-pill">DLC</span>
            </div>
            <a href="#" class="fam-view">VIEW →</a>
          </div>
        </div>

        <!-- Card 5: Sentinel — grey block placeholder (Fix 1 v2.4.2) -->
        <div class="fam-card">
          <div class="fam-card-top">
            <span class="fam-sub-tag">WALL PACK · CLASSIC</span>
            <span class="tier-badge tier-pro">PRO</span>
          </div>
          <div class="fam-img-placeholder">Sentinel</div>
          <div class="fam-card-bottom">
            <div class="fam-info">
              <div class="fam-name">Sentinel</div>
              <div class="fam-meta">120W max · 5 SKUs</div>
              <span class="dlc-pill">DLC</span>
            </div>
            <a href="#" class="fam-view">VIEW →</a>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ================================================================
       §7 ALL FAMILIES — filter rail + 23-card grid
       ================================================================ -->
  <section class="page-section bg-tint" id="sec-all-families">
    <div class="section-inner section-inner--wide">
      <div class="sec-eyebrow">All families</div>
      <h2 class="sec-title">23 families. One outdoor portfolio.</h2>
      <div class="families-layout">

        <!-- Filter rail (decorative in v2.4.0) -->
        <aside class="filter-rail">
          <div class="filter-header">
            <span class="filter-title">FILTER BY</span>
            <a href="#" class="filter-reset">Reset all</a>
          </div>

          <div class="filter-group">
            <div class="filter-group-label">Application <span class="fg-arrow">▲</span></div>
            <div class="filter-options">
              <!-- Fix 3 (v2.4.2): canonical kebab-case tokens, name=filter-application, corrected counts -->
              <label class="filter-opt"><input type="checkbox" name="filter-application" value="sports-lighting" /> Sports Lighting <span class="fg-count">(5)</span></label>
              <label class="filter-opt"><input type="checkbox" name="filter-application" value="area-light" /> Area Light <span class="fg-count">(4)</span></label>
              <label class="filter-opt"><input type="checkbox" name="filter-application" value="wall-pack" /> Wall Pack <span class="fg-count">(11)</span></label>
              <label class="filter-opt"><input type="checkbox" name="filter-application" value="flood-light" /> Flood Light <span class="fg-count">(2)</span></label>
              <label class="filter-opt"><input type="checkbox" name="filter-application" value="string-light" /> String Light <span class="fg-count">(1)</span></label>
              <label class="filter-opt"><input type="checkbox" name="filter-application" value="canopy" /> Canopy <span class="fg-count">(1)</span></label>
              <label class="filter-opt"><input type="checkbox" name="filter-application" value="cylinder" /> Cylinder <span class="fg-count">(1)</span></label>
              <label class="filter-opt"><input type="checkbox" name="filter-application" value="dock-light" /> Dock Light <span class="fg-count">(1)</span></label>
            </div>
          </div>

          <div class="filter-group">
            <div class="filter-group-label">Echelon <span class="fg-arrow">▲</span></div>
            <div class="filter-options">
              <label class="filter-opt"><input type="checkbox" disabled /> ECO <span class="fg-count">(12)</span></label>
              <label class="filter-opt"><input type="checkbox" disabled /> PRO <span class="fg-count">(9)</span></label>
              <label class="filter-opt"><input type="checkbox" disabled /> PRO+ <span class="fg-count">(3)</span></label>
            </div>
          </div>

          <div class="filter-group">
            <div class="filter-group-label">Max Wattage <span class="fg-arrow">▲</span></div>
            <div class="filter-options">
              <div class="range-placeholder">
                <div class="range-track"><div class="range-fill"></div></div>
                <div class="range-labels"><span>10W</span><span>1500W</span></div>
              </div>
            </div>
          </div>

          <div class="filter-group">
            <div class="filter-group-label">CCT <span class="fg-arrow">▲</span></div>
            <div class="filter-options">
              <label class="filter-opt"><input type="checkbox" disabled /> 2200K</label>
              <label class="filter-opt"><input type="checkbox" disabled /> 2700K</label>
              <label class="filter-opt"><input type="checkbox" disabled /> 3000K</label>
              <label class="filter-opt"><input type="checkbox" disabled /> 4000K</label>
              <label class="filter-opt"><input type="checkbox" disabled /> 5000K</label>
              <label class="filter-opt"><input type="checkbox" disabled /> 5700K</label>
            </div>
          </div>

          <div class="filter-group">
            <div class="filter-group-label">Voltage <span class="fg-arrow">▲</span></div>
            <div class="filter-options">
              <label class="filter-opt"><input type="checkbox" disabled /> 120-277V</label>
              <label class="filter-opt"><input type="checkbox" disabled /> 120V Only</label>
              <label class="filter-opt"><input type="checkbox" disabled /> 200-480V</label>
              <label class="filter-opt"><input type="checkbox" disabled /> 277-480V</label>
            </div>
          </div>

          <div class="filter-group">
            <div class="filter-group-label">DLC <span class="fg-arrow">▲</span></div>
            <div class="filter-options">
              <label class="filter-opt"><input type="checkbox" disabled /> DLC Premium</label>
              <label class="filter-opt"><input type="checkbox" disabled /> DLC Standard</label>
            </div>
          </div>

          <div class="filter-group">
            <div class="filter-group-label">TAA Compliant <span class="fg-arrow">▲</span></div>
            <div class="filter-options">
              <label class="filter-opt"><input type="checkbox" disabled /> TAA Compliant</label>
            </div>
          </div>

          <div class="filter-group">
            <div class="filter-group-label">Mount Type <span class="fg-arrow">▲</span></div>
            <div class="filter-options">
              <label class="filter-opt"><input type="checkbox" disabled /> Slip Fitter</label>
              <label class="filter-opt"><input type="checkbox" disabled /> Trunnion</label>
              <label class="filter-opt"><input type="checkbox" disabled /> Surface</label>
              <label class="filter-opt"><input type="checkbox" disabled /> Pendant</label>
              <label class="filter-opt"><input type="checkbox" disabled /> Knuckle</label>
            </div>
          </div>
        </aside>

        <!-- Grid column -->
        <div class="families-grid-col">
          <div class="grid-header">
            <span class="grid-count">Showing 23 families</span>
            <div class="sort-wrap">
              <select class="sort-select" disabled>
                <option>Sort: Featured</option>
                <option>Wattage Low to High</option>
                <option>Wattage High to Low</option>
                <option>A to Z</option>
              </select>
            </div>
          </div>

          <div class="all-fam-grid" id="all-fam-grid">
            {families.map((f) => (
              <div class="family-card af-card" data-application={f.apps}>
                <div class="af-card-top">
                  <span class="fam-sub-tag">{f.sub}</span>
                  <span class={`tier-badge tier-${f.tier === 'PRO+' ? 'proplus' : f.tier.toLowerCase()}`}>{f.tier}</span>
                </div>
                <div class="fam-img-placeholder af-placeholder">{f.name}</div>
                <div class="af-card-bottom">
                  <div class="fam-info">
                    <div class="fam-name af-name">{f.name}</div>
                    <div class="fam-meta">{f.maxW !== 'n/a' ? `${f.maxW} max · ` : ''}{f.skus} SKUs</div>
                    {f.dlc && <span class="dlc-pill">DLC</span>}
                  </div>
                  <a href="#" class="fam-view">VIEW →</a>
                </div>
              </div>
            ))}
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ================================================================
       §8 LEGACY PLACEHOLDER
       ================================================================ -->
  <section class="page-section" id="sec-legacy">
    <div class="section-inner">
      <div class="sec-eyebrow">Legacy · coming soon</div>
      <div class="legacy-box">
        <h3 class="legacy-h3">Replacement guidance for discontinued families.</h3>
        <p class="legacy-body">A cross-reference map for discontinued <BrandMark prefix="luxo" suffix="RCH" /> families — Landmark, Palisade, LDTD, LWPE, and others — is in development. When live, this section will route specifiers and contractors from a legacy SKU to the current-generation replacement family at the matching tier and sub-category.</p>
        <a href="#" class="legacy-cta">Notify me when legacy mapping is published →</a>
      </div>
    </div>
  </section>

  <!-- ================================================================
       §9 GET STARTED — 4 CTA cards
       ================================================================ -->
  <section class="page-section bg-tint" id="sec-get-started">
    <div class="section-inner">
      <div class="sec-eyebrow">Get started</div>
      <h2 class="sec-title">Four ways to move a <BrandMark prefix="luxo" suffix="RCH" /> project forward.</h2>
      <p class="sec-sub">Layout, sample, distributor, or rep — pick the one that matches what you need next.</p>
      <div class="cta-grid">
        <div class="cta-card">
          <h4 class="cta-title">Request a Layout</h4>
          <p class="cta-body">Submit your site plan. AGI32-grade photometric layout in 48 hours, no charge.</p>
          <a href="#" class="cta-link">Request Layout →</a>
        </div>
        <div class="cta-card">
          <h4 class="cta-title">Request a Sample</h4>
          <p class="cta-body">Spec samples for active projects. Ships from the nearest of 5 US warehouses.</p>
          <a href="#" class="cta-link">Request Sample →</a>
        </div>
        <div class="cta-card">
          <h4 class="cta-title">Find a Distributor</h4>
          <p class="cta-body"><BrandMark prefix="luxo" suffix="RCH" /> ships through ALG's national distributor network.</p>
          <a href="/distributor" class="cta-link">Find Distributor →</a>
        </div>
        <div class="cta-card">
          <h4 class="cta-title">Talk to a Rep</h4>
          <p class="cta-body">Regional rep coverage for spec support, pricing, and field walkthrough.</p>
          <a href="#" class="cta-link">Find a Rep →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ================================================================
       §10 RELATED COLLECTIONS — 4 cards
       ================================================================ -->
  <section class="page-section" id="sec-related">
    <div class="section-inner">
      <div class="sec-eyebrow">Related collections</div>
      <h2 class="sec-title">Explore the rest of the ALG portfolio.</h2>
      <div class="related-grid">
        <div class="related-card">
          <div class="related-eyebrow">INDOOR · COLLECTION</div>
          <div class="related-name"><BrandMark prefix="plano" suffix="RCH" /></div>
          <p class="related-desc">Commercial indoor flat panels and troffers. DLC Premium across the portfolio.</p>
          <a href="#" class="related-link">View →</a>
        </div>
        <div class="related-card">
          <div class="related-eyebrow">INDOOR · COLLECTION</div>
          <div class="related-name"><BrandMark prefix="lampar" suffix="RCH" /></div>
          <p class="related-desc">Industrial linear high-bay and low-bay luminaires. Built for warehouse and manufacturing.</p>
          <a href="#" class="related-link">View →</a>
        </div>
        <div class="related-card">
          <div class="related-eyebrow">OUTDOOR · COLLECTION</div>
          <div class="related-name"><BrandMark prefix="city" suffix="RCH" /></div>
          <p class="related-desc">Street and roadway luminaires for municipal and utility applications.</p>
          <a href="#" class="related-link">View →</a>
        </div>
        <div class="related-card">
          <div class="related-eyebrow">LAMP · COLLECTION</div>
          <div class="related-name"><BrandMark prefix="tubul" suffix="RCH" /></div>
          <p class="related-desc">T8/T5/PL/HID retrofit lamps. Drop-in replacements for legacy fluorescent.</p>
          <a href="#" class="related-link">View →</a>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  /* ── CSS VARIABLES ─────────────────────────────────────────────── */
  :root {
    --dark: #0c0d12;
    --dark2: #14161e;
    --brand: var(--color-alg-red, #F32740);
    --brand-dk: var(--color-alg-red-dark, #C41E32);
    --ink: #111418;
    --ink2: #3a3f47;
    --ink3: #8a8f99;
    --ink4: #c8ccd3;
    --bg2: #f5f5f3;
    --bg3: #eceae4;
    --radius: 3px;
    --transition: 0.2s ease;
  }

  /* ── BREADCRUMB ─────────────────────────────────────────────────── */
  .breadcrumb-band {
    padding: 12px 28px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    letter-spacing: 0.10em;
    text-transform: uppercase;
    color: var(--ink3, #8a8f99);
    background: var(--bg2, #f5f5f3);
    border-bottom: 1px solid rgba(0,0,0,0.06);
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .breadcrumb-band a { color: var(--ink3, #8a8f99); text-decoration: none; }
  .breadcrumb-band a:hover { color: var(--brand, #F32740); }
  .bc-sep { color: var(--ink4, #c8ccd3); }
  .bc-current { color: var(--ink, #111418); }

  /* ── §2 HERO ────────────────────────────────────────────────────── */
  .hero-band {
    background: #1a1f2e;
    color: #fff;
  }
  .hero-top {
    padding: 72px 28px 80px; /* Fix 3 (v2.4.1): bottom padding ~80px below pill row */
    max-width: 1200px;
    margin: 0 auto;
  }
  .hero-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    letter-spacing: 0.20em;
    text-transform: uppercase;
    color: var(--brand, #F32740);
    margin-bottom: 24px;
  }
  .hero-wordmark {
    font-family: 'Sora', sans-serif;
    font-size: clamp(72px, 10vw, 128px);
    font-weight: 900;
    letter-spacing: -0.03em;
    line-height: 0.95;
    color: #fff;
    margin: 0 0 28px;
    display: flex;
    align-items: baseline;
    flex-wrap: wrap;
    gap: 0;
  }
  /* The BrandMark Ⓐ inside the wordmark inherits the wordmark font size */
  .hero-wordmark :global(.brand-mark) {
    font-size: inherit;
    font-weight: inherit;
    letter-spacing: inherit;
    line-height: inherit;
  }
  .hero-wordmark :global(.aa) {
    color: var(--brand, #F32740);
    font-size: inherit;
  }
  .hero-desc {
    font-size: 17px;
    line-height: 1.55;
    color: rgba(255,255,255,0.70);
    max-width: 560px;
    margin-bottom: 32px;
  }
  /* Fix 5 (v2.4.1): hero-desc single line at desktop */
  @media (min-width: 1200px) {
    .hero-desc { white-space: nowrap; max-width: none; }
  }
  .hero-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }
  .hero-pill {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: #fff;
    border: 1px solid var(--brand, #F32740);
    border-radius: 2px;
    padding: 5px 10px;
    white-space: nowrap;
  }
  /* Fix 3 (v2.4.1): hero-image-strip removed — hero ends ~80px below pill row */
  /* .hero-image-strip removed; re-add in v2.4.x when hero-luxo.jpg is provided */

  /* ── §3 STAT STRIP ──────────────────────────────────────────────── */
  .stat-strip {
    background: var(--dark, #0c0d12);
    display: flex;
    align-items: stretch;
    padding: 0 28px;
    max-width: 100%;
  }
  .stat-col {
    flex: 1;
    padding: 40px 24px;
    text-align: center;
  }
  .stat-divider {
    width: 1px;
    background: rgba(255,255,255,0.10);
    align-self: stretch;
    margin: 20px 0;
  }
  .stat-num {
    font-family: 'Sora', sans-serif;
    font-size: clamp(48px, 6vw, 80px);
    font-weight: 900;
    color: #fff;
    line-height: 1;
    margin-bottom: 8px;
  }
  .stat-num.stat-red { color: var(--brand, #F32740); }
  .stat-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.60);
  }

  /* ── SHARED SECTION TOKENS ──────────────────────────────────────── */
  .page-section {
    padding: 80px 28px;
  }
  .page-section.bg-tint { background: var(--bg2, #f5f5f3); }
  .page-section.bg-dark { background: var(--dark, #0c0d12); color: #fff; }
  .section-inner {
    max-width: 1200px;
    margin: 0 auto;
  }
  .section-inner--wide {
    max-width: 1400px;
  }
  .sec-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--brand, #F32740);
    margin-bottom: 16px;
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .sec-eyebrow::before {
    content: '';
    display: inline-block;
    width: 84px;
    height: 1.5px;
    background: var(--brand, #F32740);
    flex-shrink: 0;
  }
  .sec-title {
    font-family: 'Sora', sans-serif;
    font-size: clamp(28px, 4vw, 44px);
    line-height: 1.08;
    font-weight: 800;
    letter-spacing: -0.015em;
    margin-bottom: 16px;
    color: var(--ink, #111418);
  }
  .accent { color: var(--brand, #F32740); }
  .sec-sub {
    font-size: 16px;
    line-height: 1.6;
    color: var(--ink2, #3a3f47);
    max-width: 640px;
    margin-bottom: 48px;
  }
  /* Fix 5 (v2.4.1): Featured Families subhead single line at desktop */
  #sec-featured .sec-sub {
    max-width: none;
  }
  @media (min-width: 1200px) {
    #sec-featured .sec-sub { white-space: nowrap; }
  }

  /* ── §4 APPLICATION TILES ───────────────────────────────────────── */
  .app-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
  }
  .app-tile {
    background: #f8f8f6;
    border: 1px solid rgba(0,0,0,0.08);
    border-radius: 8px;
    padding: 28px 20px 24px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    transition: box-shadow var(--transition), transform var(--transition);
    cursor: default;
  }
  .app-tile:hover {
    box-shadow: 0 6px 24px rgba(0,0,0,0.10);
    transform: translateY(-2px);
  }
  .app-icon {
    margin-bottom: 14px;
  }
  .app-name {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--ink, #111418);
    font-weight: 700;
    margin-bottom: 6px;
  }
  .app-sku {
    font-size: 12px;
    color: var(--ink3, #8a8f99);
  }

  /* ── §5 RED BANNER ──────────────────────────────────────────────── */
  .red-banner {
    background: var(--brand, #F32740);
    padding: 0 28px;
  }
  .red-banner-inner {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    align-items: stretch;
  }
  .rb-col {
    flex: 1;
    padding: 32px 20px;
    text-align: center;
  }
  .rb-div {
    width: 1px;
    background: rgba(255,255,255,0.20);
    align-self: stretch;
    margin: 16px 0;
  }
  .rb-num {
    font-family: 'Sora', sans-serif;
    font-size: clamp(32px, 4vw, 52px);
    font-weight: 900;
    color: #fff;
    line-height: 1;
    margin-bottom: 6px;
  }
  .rb-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.80);
  }

  /* ── §6 FEATURED FAMILIES ───────────────────────────────────────── */
  .featured-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 16px;
  }
  .fam-card {
    background: #0d111a;
    border-radius: 6px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    transition: transform var(--transition), box-shadow var(--transition);
  }
  .fam-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 32px rgba(0,0,0,0.40);
  }
  .fam-card-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    padding: 14px 14px 0;
    gap: 8px;
  }
  .fam-sub-tag {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.45);
    line-height: 1.4;
    flex: 1;
  }
  .tier-badge {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    letter-spacing: 0.10em;
    font-weight: 700;
    text-transform: uppercase;
    color: #fff;
    padding: 3px 7px;
    border-radius: 2px;
    white-space: nowrap;
    flex-shrink: 0;
  }
  /* Fix 2 (v2.4.1): Tier badge visual treatments */
  .tier-eco     { background: rgba(255,255,255,0.10); color: #fff; border: none; }
  .tier-pro     { background: #fff; color: #F32740; border: 1.5px solid #F32740; }
  .tier-proplus { background: #F32740; color: #fff; border: none; }
  .fam-img-wrap {
    padding: 16px 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    height: 280px;
  }
  .fam-line-img {
    max-height: 100%;
    max-width: 100%;
    object-fit: contain;
    filter: brightness(0) invert(1);
  }
  .fam-img-placeholder {
    height: 280px;
    margin: 16px 14px;
    background: rgba(255,255,255,0.04);
    border-radius: 4px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-style: italic;
    font-size: 16px;
    color: rgba(255,255,255,0.30);
  }
  .fam-card-bottom {
    padding: 12px 14px 16px;
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    margin-top: auto;
  }
  .fam-info { display: flex; flex-direction: column; gap: 4px; }
  .fam-name {
    font-family: 'Sora', sans-serif;
    font-size: 20px;
    font-weight: 700;
    color: #fff;
    line-height: 1.1;
  }
  .fam-meta {
    font-size: 13px;
    color: rgba(255,255,255,0.50);
  }
  .dlc-pill {
    display: inline-block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    letter-spacing: 0.12em;
    font-weight: 700;
    color: #2EB872;
    border: 1px solid #2EB872;
    border-radius: 2px;
    padding: 2px 6px;
    text-transform: uppercase;
    width: fit-content;
  }
  .fam-view {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    letter-spacing: 0.10em;
    font-weight: 700;
    color: var(--brand, #F32740);
    text-decoration: none;
    white-space: nowrap;
    align-self: flex-end;
  }
  .fam-view:hover { text-decoration: underline; }

  /* ── §7 ALL FAMILIES ────────────────────────────────────────────── */
  .families-layout {
    display: flex;
    gap: 32px;
    align-items: flex-start;
  }
  .filter-rail {
    width: 240px;
    flex-shrink: 0;
    position: sticky;
    top: 80px;
    background: #fff;
    border: 1px solid rgba(0,0,0,0.08);
    border-radius: 6px;
    padding: 20px;
    max-height: calc(100vh - 120px);
    overflow-y: auto;
  }
  .filter-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
    padding-bottom: 12px;
    border-bottom: 1px solid rgba(0,0,0,0.08);
  }
  .filter-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--ink, #111418);
    font-weight: 700;
  }
  .filter-reset {
    font-size: 11px;
    color: var(--brand, #F32740);
    text-decoration: none;
  }
  .filter-reset:hover { text-decoration: underline; }
  .filter-group {
    margin-bottom: 16px;
    padding-bottom: 16px;
    border-bottom: 1px solid rgba(0,0,0,0.06);
  }
  .filter-group:last-child { border-bottom: none; margin-bottom: 0; }
  .filter-group-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--ink2, #3a3f47);
    font-weight: 700;
    margin-bottom: 10px;
    display: flex;
    justify-content: space-between;
  }
  .fg-arrow { color: var(--ink3, #8a8f99); }
  .filter-options { display: flex; flex-direction: column; gap: 7px; }
  .filter-opt {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 12px;
    color: var(--ink2, #3a3f47);
    cursor: pointer; /* Fix 4 (v2.4.1): interactive */
  }
  .filter-opt input { cursor: pointer; }
  /* Fix 4 (v2.4.1): app tile active state */
  .app-tile.is-active {
    border-color: var(--brand, #F32740);
    background: #fff0f2;
    box-shadow: 0 0 0 2px var(--brand, #F32740);
  }
  .app-tile.is-active .app-icon circle,
  .app-tile.is-active .app-icon line,
  .app-tile.is-active .app-icon path,
  .app-tile.is-active .app-icon rect {
    stroke: var(--brand, #F32740);
  }
  .app-tile.is-active .app-name { color: var(--brand, #F32740); }
  /* Fix 4 (v2.4.1): app tile cursor */
  .app-tile { cursor: pointer; }
  .fg-count { color: var(--ink3, #8a8f99); margin-left: auto; }
  .range-placeholder { padding: 4px 0; }
  .range-track {
    height: 4px;
    background: rgba(0,0,0,0.10);
    border-radius: 2px;
    margin-bottom: 6px;
    position: relative;
  }
  .range-fill {
    position: absolute;
    left: 0; top: 0; bottom: 0;
    width: 100%;
    background: var(--brand, #F32740);
    border-radius: 2px;
  }
  .range-labels {
    display: flex;
    justify-content: space-between;
    font-size: 11px;
    color: var(--ink3, #8a8f99);
  }
  .families-grid-col { flex: 1; min-width: 0; }
  .grid-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
    padding-bottom: 12px;
    border-bottom: 1px solid rgba(0,0,0,0.08);
  }
  .grid-count {
    font-size: 13px;
    color: var(--ink2, #3a3f47);
  }
  .sort-select {
    font-size: 12px;
    color: var(--ink2, #3a3f47);
    border: 1px solid rgba(0,0,0,0.12);
    border-radius: 3px;
    padding: 5px 10px;
    background: #fff;
    cursor: default;
  }
  .all-fam-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 12px;
  }
  .af-card {
    background: #0d111a;
    border-radius: 5px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    transition: transform var(--transition), box-shadow var(--transition);
  }
  .af-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0,0,0,0.35);
  }
  .af-card-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    padding: 10px 10px 0;
    gap: 6px;
  }
  .fam-img-placeholder.af-placeholder {
    height: 140px;
    margin: 10px;
    font-size: 13px;
  }
  .af-card-bottom {
    padding: 8px 10px 12px;
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    margin-top: auto;
  }
  .af-name { font-size: 15px !important; }

  /* ── §8 LEGACY ──────────────────────────────────────────────────── */
  .legacy-box {
    border: 1px solid rgba(0,0,0,0.10);
    border-radius: 8px;
    background: #fafaf8;
    padding: 36px 40px;
    max-width: 800px;
  }
  .legacy-h3 {
    font-family: 'Sora', sans-serif;
    font-size: 20px;
    font-weight: 700;
    color: var(--ink, #111418);
    margin-bottom: 12px;
  }
  .legacy-body {
    font-size: 15px;
    line-height: 1.65;
    color: var(--ink2, #3a3f47);
    margin-bottom: 20px;
  }
  .legacy-cta {
    font-size: 14px;
    font-weight: 600;
    color: var(--brand, #F32740);
    text-decoration: none;
  }
  .legacy-cta:hover { text-decoration: underline; }

  /* ── §9 GET STARTED ─────────────────────────────────────────────── */
  .cta-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
  }
  .cta-card {
    background: #fff;
    border: 1px solid rgba(0,0,0,0.08);
    border-radius: 6px;
    padding: 28px 24px;
    display: flex;
    flex-direction: column;
    gap: 12px;
    transition: box-shadow var(--transition), transform var(--transition);
  }
  .cta-card:hover {
    box-shadow: 0 6px 24px rgba(0,0,0,0.08);
    transform: translateY(-2px);
  }
  .cta-title {
    font-family: 'Sora', sans-serif;
    font-size: 18px;
    font-weight: 700;
    color: var(--ink, #111418);
    margin: 0;
  }
  .cta-body {
    font-size: 14px;
    line-height: 1.55;
    color: var(--ink2, #3a3f47);
    flex: 1;
    margin: 0;
  }
  .cta-link {
    font-size: 13px;
    font-weight: 700;
    color: var(--brand, #F32740);
    text-decoration: none;
    letter-spacing: 0.02em;
  }
  .cta-link:hover { text-decoration: underline; }

  /* ── §10 RELATED COLLECTIONS ────────────────────────────────────── */
  .related-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
  }
  .related-card {
    border: 1px solid rgba(0,0,0,0.08);
    border-radius: 6px;
    padding: 28px 24px;
    display: flex;
    flex-direction: column;
    gap: 10px;
    transition: box-shadow var(--transition), transform var(--transition);
  }
  .related-card:hover {
    box-shadow: 0 6px 24px rgba(0,0,0,0.08);
    transform: translateY(-2px);
  }
  .related-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--ink3, #8a8f99);
  }
  .related-name {
    font-family: 'Sora', sans-serif;
    font-size: 22px;
    font-weight: 800;
    color: var(--ink, #111418);
    letter-spacing: -0.01em;
  }
  .related-desc {
    font-size: 13px;
    line-height: 1.55;
    color: var(--ink2, #3a3f47);
    flex: 1;
    margin: 0;
  }
  .related-link {
    font-size: 13px;
    font-weight: 700;
    color: var(--brand, #F32740);
    text-decoration: none;
  }
  .related-link:hover { text-decoration: underline; }

  /* ── RESPONSIVE ─────────────────────────────────────────────────── */
  @media (max-width: 1399px) {
    .all-fam-grid { grid-template-columns: repeat(3, 1fr); }
  }
  @media (max-width: 1199px) {
    .featured-grid { grid-template-columns: repeat(3, 1fr); }
    .app-grid { grid-template-columns: repeat(4, 1fr); }
    .cta-grid { grid-template-columns: repeat(2, 1fr); }
    .related-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 1023px) {
    .families-layout { flex-direction: column; }
    .filter-rail { width: 100%; position: static; max-height: none; }
    .all-fam-grid { grid-template-columns: repeat(2, 1fr); }
    .featured-grid { grid-template-columns: repeat(2, 1fr); }
    .stat-strip { flex-wrap: wrap; }
    .stat-col { min-width: 50%; }
    .rb-col { padding: 24px 12px; }
    .rb-num { font-size: 32px; }
  }
  @media (max-width: 767px) {
    .hero-wordmark { font-size: clamp(52px, 14vw, 72px); }
    .app-grid { grid-template-columns: repeat(2, 1fr); }
    .featured-grid { grid-template-columns: 1fr; }
    .all-fam-grid { grid-template-columns: 1fr; }
    .cta-grid { grid-template-columns: 1fr; }
    .related-grid { grid-template-columns: 1fr; }
    .red-banner-inner { flex-wrap: wrap; }
    .rb-col { min-width: 33%; }
    .rb-div { display: none; }
    .legacy-box { padding: 24px 20px; }
  }
  /* Fix 3 (v2.4.2): hidden family-cards */
  .family-card[style*="display: none"] { display: none !important; }
  /* Fix 5 (v2.4.2): sticky right-rail anchor nav */
  .anchor-nav {
    position: fixed;
    right: 24px;
    top: 50%;
    transform: translateY(-50%);
    display: flex;
    flex-direction: column;
    gap: 10px;
    z-index: 900;
  }
  @media (max-width: 1023px) { .anchor-nav { display: none; } }
  .anchor-dot-wrap {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: flex-end;
  }
  .anchor-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: rgba(0,0,0,0.28);
    cursor: pointer;
    transition: width 0.2s ease, height 0.2s ease, background 0.2s ease;
    flex-shrink: 0;
  }
  .anchor-dot.active {
    width: 10px;
    height: 10px;
    background: var(--brand, #F32740);
  }
  .anchor-dot-wrap:hover .anchor-dot {
    background: var(--brand, #F32740);
  }
  .anchor-label {
    position: absolute;
    right: 18px;
    white-space: nowrap;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #fff;
    background: rgba(0,0,0,0.72);
    padding: 3px 8px;
    border-radius: 3px;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.2s ease;
  }
  .anchor-dot-wrap:hover .anchor-label { opacity: 1; }
</style>

<!-- Fix 5 (v2.4.2): sticky right-rail anchor nav -->
<nav class="anchor-nav" aria-label="Page sections">
  <div class="anchor-dot-wrap"><span class="anchor-label">Overview</span><div class="anchor-dot" data-target="sec-hero"></div></div>
  <div class="anchor-dot-wrap"><span class="anchor-label">Quick stats</span><div class="anchor-dot" data-target="sec-stats"></div></div>
  <div class="anchor-dot-wrap"><span class="anchor-label">Browse by application</span><div class="anchor-dot" data-target="sec-apps"></div></div>
  <div class="anchor-dot-wrap"><span class="anchor-label">Featured families</span><div class="anchor-dot" data-target="sec-featured"></div></div>
  <div class="anchor-dot-wrap"><span class="anchor-label">All families</span><div class="anchor-dot" data-target="sec-all-families"></div></div>
  <div class="anchor-dot-wrap"><span class="anchor-label">Legacy</span><div class="anchor-dot" data-target="sec-legacy"></div></div>
  <div class="anchor-dot-wrap"><span class="anchor-label">Get started</span><div class="anchor-dot" data-target="sec-get-started"></div></div>
  <div class="anchor-dot-wrap"><span class="anchor-label">Related collections</span><div class="anchor-dot" data-target="sec-related"></div></div>
</nav>

<script>
  /* Fix 3 (v2.4.2) - Application filter (canonical kebab-case tokens).
   * Checkbox name=filter-application, value=kebab-case token.
   * Each .family-card has data-application with space-separated tokens.
   * OR logic: card visible if any checked token is in its token list. */
  function applyApplicationFilter() {
    var checked = Array.from(document.querySelectorAll('input[name="filter-application"]:checked'))
      .map(function (cb) { return cb.value; });
    var cards = document.querySelectorAll('.family-card[data-application]');
    var visibleCount = 0;
    cards.forEach(function (card) {
      var apps = card.dataset.application.split(/\s+/);
      var visible = checked.length === 0 || checked.some(function (c) { return apps.indexOf(c) !== -1; });
      card.style.display = visible ? '' : 'none';
      if (visible) visibleCount++;
    });
    var countEl = document.querySelector('.grid-count');
    if (countEl) {
      countEl.textContent = visibleCount === 23
        ? 'Showing 23 families'
        : 'Showing ' + visibleCount + ' families';
    }
    /* Sync app tile active state */
    document.querySelectorAll('.app-tile[data-app]').forEach(function (tile) {
      var token = tile.dataset.app
        .toLowerCase()
        .replace(/\s+/g, '-');
      tile.classList.toggle('is-active', checked.indexOf(token) !== -1);
    });
  }

  document.querySelectorAll('input[name="filter-application"]').forEach(function (cb) {
    cb.addEventListener('change', applyApplicationFilter);
  });

  var resetBtn = document.querySelector('.filter-reset');
  if (resetBtn) {
    resetBtn.addEventListener('click', function (e) {
      e.preventDefault();
      document.querySelectorAll('input[name="filter-application"]:checked').forEach(function (cb) {
        cb.checked = false;
      });
      applyApplicationFilter();
    });
  }

  /* App tile click: set matching checkbox + apply + scroll */
  document.querySelectorAll('.app-tile[data-app]').forEach(function (tile) {
    tile.addEventListener('click', function () {
      var token = tile.dataset.app.toLowerCase().replace(/\s+/g, '-');
      var cb = document.querySelector('input[name="filter-application"][value="' + token + '"]');
      if (cb) { cb.checked = !cb.checked; }
      applyApplicationFilter();
      var sec = document.getElementById('sec-all-families');
      if (sec) { sec.scrollIntoView({ behavior: 'smooth', block: 'start' }); }
    });
  });

  /* Fix 5 (v2.4.2) - Sticky right-rail anchor nav.
   * Dots scroll to sections; active dot tracks viewport via IntersectionObserver. */
  (function () {
    var dots = document.querySelectorAll('.anchor-dot[data-target]');

    dots.forEach(function (dot) {
      dot.addEventListener('click', function () {
        var sec = document.getElementById(dot.dataset.target);
        if (sec) { sec.scrollIntoView({ behavior: 'smooth', block: 'start' }); }
      });
    });

    var sections = document.querySelectorAll('section[id]');
    if (!sections.length) return;

    var observer = new IntersectionObserver(function (entries) {
      entries.forEach(function (entry) {
        if (entry.isIntersecting) {
          var id = entry.target.id;
          dots.forEach(function (d) {
            d.classList.toggle('active', d.dataset.target === id);
          });
        }
      });
    }, { threshold: 0.3, rootMargin: '-20% 0% -20% 0%' });

    sections.forEach(function (s) { observer.observe(s); });
  })();
</script>
