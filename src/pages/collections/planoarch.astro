---
import BaseLayout from '../../layouts/BaseLayout.astro';
import BrandMark from '../../components/BrandMark.astro';
import CollectionAnchorNav from '../../components/CollectionAnchorNav.astro';

/* v2.5.3: planoⒶRCH collection page — roster update + canonical alignment.
 * Fix 1 (v2.5.3): Remove selectⒶRCH and Stellar. Updated counts to 15/125.
 * Fix 3 (v2.5.3): §7 All Families — white outer bg + visible title + "FILTER BY" header matching luxo.
 * Fix 4 (v2.5.3): §4 Browse — white bg + light tiles + headline removed.
 * Fix 5 (v2.5.3): §4 tile order alphabetical; updated SKU counts.
 * Fix 7 (v2.5.3): Sort by sub-category A→Z, then ECO→PRO→PRO+, then A→Z within tier.
 * Fix 8 (v2.5.3): Tier badges static (no hover-conditional opacity).
 * Fix 10 (v2.5.3): Hero pill "8 APPLICATIONS".
 * Fix 11 (v2.5.3): Related Collections — luxoarch live link; lampar/city/tubul Coming soon. */
const _TIER_ORDER: Record<string, number> = { 'ECO': 0, 'PRO': 1, 'PRO+': 2 };
const families = ([
  /* Fix 1 (v2.5.3): selectⒶRCH and Stellar removed. 15 families remain. */
  { sub: 'ARCHITECTURAL LINEAR', tier: 'ECO',  name: 'Lara',              maxW: '60W',  skus: 6,  dlc: true,  apps: 'architectural-linear' },
  { sub: 'ARCHITECTURAL LINEAR', tier: 'PRO+', name: 'Luna',              maxW: '50W',  skus: 5,  dlc: true,  apps: 'architectural-linear' },
  { sub: 'DOWNLIGHT',            tier: 'ECO',  name: 'Solstice',          maxW: '22W',  skus: 7,  dlc: true,  apps: 'downlight' },
  { sub: 'DOWNLIGHT',            tier: 'PRO',  name: 'Solstice SafeZone', maxW: '26W',  skus: 4,  dlc: true,  apps: 'downlight' },
  { sub: 'DOWNLIGHT',            tier: 'PRO+', name: 'Astra',             maxW: '18W',  skus: 27, dlc: true,  apps: 'downlight' },
  { sub: 'DOWNLIGHT',            tier: 'PRO+', name: 'Spectra',           maxW: '30W',  skus: 3,  dlc: true,  apps: 'downlight' },
  { sub: 'PANEL',                tier: 'ECO',  name: 'proⒶRCH-II',       maxW: '30W',  skus: 7,  dlc: true,  apps: 'panel' },
  { sub: 'PANEL',                tier: 'PRO+', name: 'proⒶRCH-III',      maxW: '30W',  skus: 13, dlc: true,  apps: 'panel' },
  { sub: 'RETROFIT PANEL',       tier: 'PRO',  name: 'retroⒶRCH-P1',     maxW: '26W',  skus: 12, dlc: true,  apps: 'retrofit-panel' },
  { sub: 'RETROFIT TROFFER',     tier: 'PRO',  name: 'retroⒶRCH-T1',     maxW: '26W',  skus: 12, dlc: true,  apps: 'retrofit-troffer' },
  { sub: 'TRACK LIGHT',          tier: 'ECO',  name: 'Trackstar',         maxW: '20W',  skus: 4,  dlc: false, apps: 'track-light' },
  { sub: 'TROFFER',              tier: 'ECO',  name: 'proⒶRCH-T1',       maxW: '26W',  skus: 13, dlc: true,  apps: 'troffer' },
  { sub: 'TROFFER',              tier: 'PRO',  name: 'proⒶRCH-T2',       maxW: '30W',  skus: 2,  dlc: true,  apps: 'troffer' },
  { sub: 'TROFFER',              tier: 'PRO+', name: 'Luxmark',           maxW: '35W',  skus: 6,  dlc: true,  apps: 'troffer' },
  { sub: 'WRAPAROUND',           tier: 'PRO',  name: 'Waymark',           maxW: '35W',  skus: 4,  dlc: true,  apps: 'wraparound' },
] as const).slice().sort((a: any, b: any) => {
  /* Fix 7: sub A→Z, then tier ECO→PRO→PRO+, then name A→Z */
  const sd = a.sub.localeCompare(b.sub);
  if (sd !== 0) return sd;
  const td = (_TIER_ORDER[a.tier] ?? 99) - (_TIER_ORDER[b.tier] ?? 99);
  return td !== 0 ? td : a.name.localeCompare(b.name);
});
---
<BaseLayout
  title="planoARCH Indoor Collection — Archipelago Lighting Group"
  description="Commercial indoor lighting for offices, retail, and healthcare. 15 active families, 125 active SKUs."
  bodyClass="page-collection page-planoarch"
>

  <!-- §1 BREADCRUMB -->
  <div class="breadcrumb-band">
    <a href="/">Home</a>
    <span class="bc-sep">›</span>
    <a href="/collections">Products</a>
    <span class="bc-sep">›</span>
    <a href="/collections/indoor">Indoor</a>
    <span class="bc-sep">›</span>
    <span class="bc-current">plano<BrandMark prefix="" suffix="RCH" /></span>
  </div>

  <!-- §2 HERO -->
  <section class="hero-band" id="sec-hero">
    <div class="hero-top">
      <div class="hero-eyebrow">INDOOR · COLLECTION</div>
      <h1 class="hero-wordmark">PLANO<BrandMark prefix="" suffix="RCH" /></h1>
      <!-- Fix 1 (v2.5.3): Updated counts 15 families / 125 SKUs -->
      <p class="hero-desc">Commercial indoor lighting for offices, retail, and healthcare. 15 active families, 125 active SKUs.</p>
      <div class="hero-pills">
        <span class="hero-pill">DLC PREMIUM</span>
        <span class="hero-pill">TAA AVAILABLE</span>
        <span class="hero-pill">5 US WAREHOUSES</span>
        <span class="hero-pill">3-DAY DELIVERY</span>
        <!-- Fix 10 (v2.5.3): "8 SUB-CATEGORIES" → "8 APPLICATIONS" -->
        <span class="hero-pill">8 APPLICATIONS</span>
      </div>
    </div>
  </section>

  <!-- §3 STAT STRIP -->
  <section class="stat-strip" id="sec-stats">
    <div class="stat-strip-inner">
      <!-- Fix 1 (v2.5.3): 17→15, 132→125 -->
      <div class="stat-col"><span class="stat-num">15</span><span class="stat-lbl">Fixture Families</span></div>
      <div class="stat-divider"></div>
      <div class="stat-col"><span class="stat-num">125</span><span class="stat-lbl">Active SKUs</span></div>
      <div class="stat-divider"></div>
      <div class="stat-col"><span class="stat-num">8</span><span class="stat-lbl">Sub-categories</span></div>
      <div class="stat-divider"></div>
      <div class="stat-col"><span class="stat-num stat-red">DLC</span><span class="stat-lbl">Premium Listed</span></div>
    </div>
  </section>

  <!-- §4 BROWSE BY SUB-CATEGORY
       Fix 4 (v2.5.3): White bg + light tiles (matching luxo canonical). Headline removed.
       Fix 5 (v2.5.3): Tiles in alphabetical order; updated SKU counts. -->
  <section class="page-section sec-browse" id="sec-apps">
    <div class="section-inner">
      <div class="sec-eyebrow">Browse by sub-category</div>
      <!-- Fix 4 (v2.5.3): "Eight indoor sub-categories." headline REMOVED -->
      <div class="app-grid">

        <!-- 1. Architectural Linear (11 SKUs) -->
        <button class="app-tile" data-app="architectural-linear" aria-pressed="false">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <rect x="2" y="11" width="20" height="2" rx="1"/>
          </svg>
          <span class="app-label">Architectural Linear</span>
          <span class="app-count">11 SKUs</span>
        </button>

        <!-- 2. Downlight (41 SKUs) -->
        <button class="app-tile" data-app="downlight" aria-pressed="false">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <circle cx="12" cy="8" r="4"/>
            <path d="M 8 12 L 6 20 M 16 12 L 18 20"/>
          </svg>
          <span class="app-label">Downlight</span>
          <!-- Fix 5 (v2.5.3): 42→41 (Stellar removed) -->
          <span class="app-count">41 SKUs</span>
        </button>

        <!-- 3. Panel (20 SKUs) -->
        <button class="app-tile" data-app="panel" aria-pressed="false">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <rect x="3" y="9" width="18" height="6" rx="0.5"/>
          </svg>
          <span class="app-label">Panel</span>
          <span class="app-count">20 SKUs</span>
        </button>

        <!-- 4. Retrofit Panel (12 SKUs) -->
        <button class="app-tile" data-app="retrofit-panel" aria-pressed="false">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <rect x="3" y="11" width="14" height="4" rx="0.5"/>
            <path d="M 18 8 L 21 8 L 21 11 M 21 8 L 18 11"/>
          </svg>
          <span class="app-label">Retrofit Panel</span>
          <span class="app-count">12 SKUs</span>
        </button>

        <!-- 5. Retrofit Troffer (12 SKUs) -->
        <button class="app-tile" data-app="retrofit-troffer" aria-pressed="false">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <rect x="3" y="9" width="14" height="8" rx="1"/>
            <line x1="3" y1="13" x2="17" y2="13"/>
            <path d="M 18 5 L 21 5 L 21 8 M 21 5 L 18 8"/>
          </svg>
          <span class="app-label">Retrofit Troffer</span>
          <span class="app-count">12 SKUs</span>
        </button>

        <!-- 6. Track Light (4 SKUs) -->
        <button class="app-tile" data-app="track-light" aria-pressed="false">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <line x1="3" y1="8" x2="21" y2="8"/>
            <circle cx="7" cy="13" r="2"/>
            <circle cx="12" cy="13" r="2"/>
            <circle cx="17" cy="13" r="2"/>
          </svg>
          <span class="app-label">Track Light</span>
          <span class="app-count">4 SKUs</span>
        </button>

        <!-- 7. Troffer (21 SKUs) -->
        <button class="app-tile" data-app="troffer" aria-pressed="false">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <rect x="3" y="6" width="18" height="12" rx="1"/>
            <line x1="3" y1="10" x2="21" y2="10"/>
            <line x1="3" y1="14" x2="21" y2="14"/>
          </svg>
          <span class="app-label">Troffer</span>
          <!-- Fix 5 (v2.5.3): 27→21 (selectⒶRCH removed) -->
          <span class="app-count">21 SKUs</span>
        </button>

        <!-- 8. Wraparound (4 SKUs) -->
        <button class="app-tile" data-app="wraparound" aria-pressed="false">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <rect x="4" y="8" width="16" height="8" rx="3"/>
          </svg>
          <span class="app-label">Wraparound</span>
          <span class="app-count">4 SKUs</span>
        </button>

      </div>
    </div>
  </section>

  <!-- §5 RED STAT BANNER -->
  <section class="red-banner" id="sec-red-banner">
    <div class="red-banner-inner">
      <!-- Fix 1 (v2.5.3): 17→15, 132→125 -->
      <div class="rb-col"><span class="rb-num">15</span><span class="rb-lbl">Families</span></div>
      <div class="rb-div"></div>
      <div class="rb-col"><span class="rb-num">125</span><span class="rb-lbl">Active SKUs</span></div>
      <div class="rb-div"></div>
      <div class="rb-col"><span class="rb-num">8</span><span class="rb-lbl">Sub-categories</span></div>
      <div class="rb-div"></div>
      <div class="rb-col"><span class="rb-num">DLC</span><span class="rb-lbl">Premium</span></div>
      <div class="rb-div"></div>
      <div class="rb-col"><span class="rb-num">TAA</span><span class="rb-lbl">Available</span></div>
      <div class="rb-div"></div>
      <div class="rb-col"><span class="rb-num">5 US</span><span class="rb-lbl">Warehouses</span></div>
    </div>
  </section>

  <!-- §6 FEATURED FAMILIES — black bg canonical (matching luxo) -->
  <section class="page-section sec-featured" id="sec-featured">
    <div class="section-inner">
      <div class="sec-eyebrow">Featured families</div>
      <h2 class="sec-title">Five flagship families for the commercial interior.</h2>
      <p class="sec-sub">The most-specified plano<BrandMark prefix="" suffix="RCH" /> families across downlight, linear, troffer, panel, and track.</p>
      <div class="featured-grid">

        <!-- Card 1: Astra PRO+ Downlight -->
        <div class="feat-card">
          <div class="feat-card-top">
            <span class="feat-sub-tag">DOWNLIGHT</span>
            <!-- Fix 8 (v2.5.3): tier badge always visible — no hover-conditional opacity -->
            <span class="tier-badge pro-plus">PRO+</span>
          </div>
          <div class="fam-img-placeholder feat-placeholder"></div>
          <div class="feat-card-body">
            <h3 class="feat-name">Astra</h3>
            <div class="feat-meta">
              <span class="feat-maxw">18W max</span>
              <span class="feat-skus">27 SKUs</span>
              <span class="feat-dlc dlc-pill">DLC</span>
            </div>
            <a href="#" class="feat-cta">VIEW →</a>
          </div>
        </div>

        <!-- Card 2: Luna PRO+ Architectural Linear -->
        <div class="feat-card">
          <div class="feat-card-top">
            <span class="feat-sub-tag">ARCHITECTURAL LINEAR</span>
            <span class="tier-badge pro-plus">PRO+</span>
          </div>
          <div class="fam-img-placeholder feat-placeholder"></div>
          <div class="feat-card-body">
            <h3 class="feat-name">Luna</h3>
            <div class="feat-meta">
              <span class="feat-maxw">50W max</span>
              <span class="feat-skus">5 SKUs</span>
              <span class="feat-dlc dlc-pill">DLC</span>
            </div>
            <a href="#" class="feat-cta">VIEW →</a>
          </div>
        </div>

        <!-- Card 3: Luxmark PRO+ Troffer -->
        <div class="feat-card">
          <div class="feat-card-top">
            <span class="feat-sub-tag">TROFFER</span>
            <span class="tier-badge pro-plus">PRO+</span>
          </div>
          <div class="fam-img-placeholder feat-placeholder"></div>
          <div class="feat-card-body">
            <h3 class="feat-name">Luxmark</h3>
            <div class="feat-meta">
              <span class="feat-maxw">35W max</span>
              <span class="feat-skus">6 SKUs</span>
              <span class="feat-dlc dlc-pill">DLC</span>
            </div>
            <a href="#" class="feat-cta">VIEW →</a>
          </div>
        </div>

        <!-- Card 4: proⒶRCH-III PRO+ Panel -->
        <div class="feat-card">
          <div class="feat-card-top">
            <span class="feat-sub-tag">PANEL</span>
            <span class="tier-badge pro-plus">PRO+</span>
          </div>
          <div class="fam-img-placeholder feat-placeholder"></div>
          <div class="feat-card-body">
            <h3 class="feat-name"><BrandMark prefix="pro" suffix="RCH-III" /></h3>
            <div class="feat-meta">
              <span class="feat-maxw">30W max</span>
              <span class="feat-skus">13 SKUs</span>
              <span class="feat-dlc dlc-pill">DLC</span>
            </div>
            <a href="#" class="feat-cta">VIEW →</a>
          </div>
        </div>

        <!-- Card 5: Trackstar ECO Track Light -->
        <div class="feat-card">
          <div class="feat-card-top">
            <span class="feat-sub-tag">TRACK LIGHT</span>
            <span class="tier-badge eco">ECO</span>
          </div>
          <div class="fam-img-placeholder feat-placeholder"></div>
          <div class="feat-card-body">
            <h3 class="feat-name">Trackstar</h3>
            <div class="feat-meta">
              <span class="feat-maxw">20W max</span>
              <span class="feat-skus">4 SKUs</span>
            </div>
            <a href="#" class="feat-cta">VIEW →</a>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- §7 ALL FAMILIES
       Fix 3 (v2.5.3): White outer bg + "ALL FAMILIES" eyebrow + headline + "FILTER BY" header.
       Fix 7 (v2.5.3): Sort by sub-category A→Z, then ECO→PRO→PRO+, then A→Z. -->
  <section class="page-section sec-all" id="sec-all-families">
    <div class="section-inner section-inner--wide">
      <!-- Fix 3 (v2.5.3): Added eyebrow + headline matching luxo canonical -->
      <div class="sec-eyebrow">All families</div>
      <h2 class="sec-title sec-title-dark">15 families. One indoor portfolio.</h2>
      <div class="families-layout">

        <!-- LEFT: Filter rail — Fix 3: added FILTER BY header matching luxo -->
        <aside class="filter-rail">
          <div class="filter-header">
            <span class="filter-title">FILTER BY</span>
            <button class="filter-reset-btn" id="reset-filter">Reset all</button>
          </div>

          <div class="filter-group">
            <h3 class="filter-group-title">Sub-category</h3>
            <!-- Fix 7: family counts per sub-category from canonical card order -->
            <label class="filter-opt"><input type="checkbox" class="app-filter-cb" name="filter-subcategory" value="architectural-linear"> Architectural Linear <span class="filter-count">(2)</span></label>
            <label class="filter-opt"><input type="checkbox" class="app-filter-cb" name="filter-subcategory" value="downlight"> Downlight <span class="filter-count">(4)</span></label>
            <label class="filter-opt"><input type="checkbox" class="app-filter-cb" name="filter-subcategory" value="panel"> Panel <span class="filter-count">(2)</span></label>
            <label class="filter-opt"><input type="checkbox" class="app-filter-cb" name="filter-subcategory" value="retrofit-panel"> Retrofit Panel <span class="filter-count">(1)</span></label>
            <label class="filter-opt"><input type="checkbox" class="app-filter-cb" name="filter-subcategory" value="retrofit-troffer"> Retrofit Troffer <span class="filter-count">(1)</span></label>
            <label class="filter-opt"><input type="checkbox" class="app-filter-cb" name="filter-subcategory" value="track-light"> Track Light <span class="filter-count">(1)</span></label>
            <label class="filter-opt"><input type="checkbox" class="app-filter-cb" name="filter-subcategory" value="troffer"> Troffer <span class="filter-count">(3)</span></label>
            <label class="filter-opt"><input type="checkbox" class="app-filter-cb" name="filter-subcategory" value="wraparound"> Wraparound <span class="filter-count">(1)</span></label>
          </div>
          <div class="filter-group">
            <h3 class="filter-group-title">Echelon</h3>
            <!-- Fix 7: ECO=5, PRO=5, PRO+=5 -->
            <label class="filter-opt"><input type="checkbox" disabled> ECO <span class="filter-count">(5)</span></label>
            <label class="filter-opt"><input type="checkbox" disabled> PRO <span class="filter-count">(5)</span></label>
            <label class="filter-opt"><input type="checkbox" disabled> PRO+ <span class="filter-count">(5)</span></label>
          </div>
          <div class="filter-group">
            <h3 class="filter-group-title">Max Wattage</h3>
            <div class="filter-range-wrap">
              <input type="range" min="10" max="60" value="60" disabled class="filter-range" />
              <div class="filter-range-labels"><span>10W</span><span>60W</span></div>
            </div>
          </div>
          <div class="filter-group">
            <h3 class="filter-group-title">CCT</h3>
            <label class="filter-opt"><input type="checkbox" disabled> 2700K</label>
            <label class="filter-opt"><input type="checkbox" disabled> 3000K</label>
            <label class="filter-opt"><input type="checkbox" disabled> 3500K</label>
            <label class="filter-opt"><input type="checkbox" disabled> 4000K</label>
            <label class="filter-opt"><input type="checkbox" disabled> 5000K</label>
          </div>
          <div class="filter-group">
            <h3 class="filter-group-title">Voltage</h3>
            <label class="filter-opt"><input type="checkbox" disabled> 120-277V</label>
            <label class="filter-opt"><input type="checkbox" disabled> 120V Only</label>
          </div>
          <div class="filter-group">
            <h3 class="filter-group-title">DLC</h3>
            <label class="filter-opt"><input type="checkbox" disabled> DLC Premium</label>
            <label class="filter-opt"><input type="checkbox" disabled> DLC Standard</label>
          </div>
          <div class="filter-group">
            <h3 class="filter-group-title">TAA Compliant</h3>
            <label class="filter-opt"><input type="checkbox" disabled> TAA Compliant</label>
          </div>
          <div class="filter-group">
            <h3 class="filter-group-title">Mount Type</h3>
            <label class="filter-opt"><input type="checkbox" disabled> Recessed</label>
            <label class="filter-opt"><input type="checkbox" disabled> Surface</label>
            <label class="filter-opt"><input type="checkbox" disabled> Suspended</label>
            <label class="filter-opt"><input type="checkbox" disabled> Track</label>
          </div>
        </aside>

        <!-- RIGHT: Grid -->
        <div class="af-grid-col">
          <div class="grid-header">
            <!-- Fix 1 (v2.5.3): 17→15 -->
            <span class="grid-count" id="grid-count">Showing 15 families</span>
            <div class="sort-wrap">
              <!-- Fix 7 (v2.5.3): Sort label updated -->
              <select class="sort-select" disabled>
                <option>Sort: Application → ECO→PRO+</option>
                <option>Wattage Low to High</option>
                <option>Wattage High to Low</option>
                <option>A to Z</option>
              </select>
            </div>
          </div>
          <div class="all-fam-grid" id="all-fam-grid">
            {families.map((f: any) => (
              <div class="af-card" data-subcategory={f.apps}>
                <div class="af-card-top">
                  <span class="fam-sub-tag">{f.sub.split(' · ')[0]}</span>
                  <!-- Fix 8 (v2.5.3): tier badge always visible — class-based, no hover-conditional opacity -->
                  <span class={`tier-badge ${f.tier === 'PRO+' ? 'pro-plus' : f.tier.toLowerCase()}`}>{f.tier}</span>
                </div>
                <div class="fam-img-placeholder af-placeholder"></div>
                <div class="af-card-bottom">
                  <div>
                    <span class="af-sub">{f.sub}</span>
                    <h3 class="af-name">{f.name.includes('Ⓐ') ? <Fragment set:html={f.name.replace(/Ⓐ/g, '<span class="aa">Ⓐ</span>')} /> : f.name}</h3>
                    <div class="af-meta">
                      <span class="af-maxw">{f.maxW}</span>
                      <span class="af-skus">{f.skus} SKUs</span>
                      {f.dlc && <span class="af-dlc dlc-pill">DLC</span>}
                    </div>
                  </div>
                  <a href="#" class="af-cta">VIEW →</a>
                </div>
              </div>
            ))}
          </div>
          <div class="af-no-results" id="no-results" style="display:none">
            <p>No families match the selected filters. <button class="reset-inline" id="reset-inline">Reset filters</button></p>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- §8 LEGACY -->
  <section class="page-section sec-legacy" id="sec-legacy">
    <div class="section-inner">
      <div class="sec-eyebrow">Legacy · Coming soon</div>
      <h2 class="sec-title">Replacement guidance for discontinued families.</h2>
      <div class="legacy-box">
        <h3 class="legacy-h3">Cross-reference map</h3>
        <p class="legacy-body">A cross-reference map for discontinued plano<BrandMark prefix="" suffix="RCH" /> families will route specifiers and contractors from a legacy SKU to the current-generation replacement family at the matching tier and sub-category. Expected publishing window: Q2 2026.</p>
        <a href="#" class="legacy-cta">Notify me when legacy mapping is published →</a>
      </div>
    </div>
  </section>

  <!-- §9 GET STARTED -->
  <section class="page-section sec-get-started" id="sec-get-started">
    <div class="section-inner">
      <div class="sec-eyebrow">Get started</div>
      <h2 class="sec-title">Four ways to move a plano<BrandMark prefix="" suffix="RCH" /> project forward.</h2>
      <p class="sec-sub">Layout, sample, distributor, or rep — pick the one that matches what you need next.</p>
      <div class="cta-grid">
        <div class="cta-card">
          <h3 class="cta-title">Request a Layout</h3>
          <p class="cta-body">Submit your floor plan. AGI32-grade photometric layout in 48 hours, no charge.</p>
          <a href="#" class="cta-link">Request Layout →</a>
        </div>
        <div class="cta-card">
          <h3 class="cta-title">Request a Sample</h3>
          <p class="cta-body">Spec samples for active projects. Ships from the nearest of 5 US warehouses.</p>
          <a href="#" class="cta-link">Request Sample →</a>
        </div>
        <div class="cta-card">
          <h3 class="cta-title">Find a Distributor</h3>
          <p class="cta-body">plano<BrandMark prefix="" suffix="RCH" /> ships through ALG's national distributor network.</p>
          <a href="/distributor" class="cta-link">Find Distributor →</a>
        </div>
        <div class="cta-card">
          <h3 class="cta-title">Talk to a Rep</h3>
          <p class="cta-body">Regional rep coverage for spec support, pricing, and field walkthrough.</p>
          <a href="#" class="cta-link">Find a Rep →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- §10 RELATED COLLECTIONS
       Fix 11 (v2.5.3): luxoarch live link; lampar/city/tubul Coming soon (disabled). -->
  <section class="page-section sec-related" id="sec-related">
    <div class="section-inner">
      <div class="sec-eyebrow">Related collections</div>
      <h2 class="sec-title">Explore the rest of the ALG portfolio.</h2>
      <div class="related-grid">

        <!-- Card 1: luxoARCH — LIVE -->
        <div class="related-card">
          <div class="related-eyebrow">OUTDOOR · COLLECTION</div>
          <div class="related-name"><BrandMark prefix="luxo" suffix="RCH" /></div>
          <!-- Fix 1 (v2.5.3): Updated luxo counts in description -->
          <p class="related-desc">Outdoor lighting for the commercial perimeter. 22 active families, 142 active SKUs.</p>
          <a href="/collections/luxoarch" class="related-link">View →</a>
        </div>

        <!-- Card 2: lamparARCH — Coming soon -->
        <div class="related-card related-card--disabled">
          <div class="related-eyebrow">INDOOR · COLLECTION</div>
          <div class="related-name"><BrandMark prefix="lampar" suffix="RCH" /></div>
          <p class="related-desc">Industrial linear high-bay and low-bay luminaires. Built for warehouse and manufacturing.</p>
          <span class="related-link-disabled">Coming soon</span>
        </div>

        <!-- Card 3: cityARCH — Coming soon -->
        <div class="related-card related-card--disabled">
          <div class="related-eyebrow">OUTDOOR · COLLECTION</div>
          <div class="related-name"><BrandMark prefix="city" suffix="RCH" /></div>
          <p class="related-desc">Street and roadway luminaires for municipal and utility applications.</p>
          <span class="related-link-disabled">Coming soon</span>
        </div>

        <!-- Card 4: tubulARCH — Coming soon -->
        <div class="related-card related-card--disabled">
          <div class="related-eyebrow">LAMP · COLLECTION</div>
          <div class="related-name"><BrandMark prefix="tubul" suffix="RCH" /></div>
          <p class="related-desc">T8/T5/PL/HID retrofit lamps. Drop-in replacements for legacy fluorescent.</p>
          <span class="related-link-disabled">Coming soon</span>
        </div>

      </div>
    </div>
  </section>

  <!-- Shared CollectionAnchorNav component -->
  <CollectionAnchorNav sections={[
    { id: 'sec-hero',         label: 'Overview' },
    { id: 'sec-stats',        label: 'Quick stats' },
    { id: 'sec-apps',         label: 'Applications' },
    { id: 'sec-featured',     label: 'Featured' },
    { id: 'sec-all-families', label: 'All families' },
    { id: 'sec-legacy',       label: 'Legacy' },
    { id: 'sec-get-started',  label: 'Get started' },
    { id: 'sec-related',      label: 'Related' },
  ]} />

</BaseLayout>

<style>
  /* ── CSS VARIABLES ─────────────────────────────────────────────────── */
  :root {
    --dark:       #0c0d12;
    --dark2:      #14161e;
    --brand:      var(--color-alg-red, #F32740);
    --brand-dk:   var(--color-alg-red-dark, #C41E32);
    --ink:        #111418;
    --ink2:       #3a3f47;
    --ink3:       #8a8f99;
    --ink4:       #c8ccd3;
    --bg2:        #f5f5f3;
    --bg3:        #eceae4;
    --radius:     3px;
    --transition: 0.2s ease;
  }

  /* ── BREADCRUMB ─────────────────────────────────────────────────────── */
  .breadcrumb-band {
    padding: 12px 28px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    letter-spacing: 0.10em;
    text-transform: uppercase;
    color: var(--ink3, #8a8f99);
    background: var(--bg2, #f5f5f3);
    border-bottom: 1px solid rgba(0,0,0,0.06);
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .breadcrumb-band a { color: var(--ink3, #8a8f99); text-decoration: none; }
  .breadcrumb-band a:hover { color: var(--brand, #F32740); }
  .bc-sep { color: var(--ink4, #c8ccd3); }
  .bc-current { color: var(--ink, #111418); }

  /* ── §2 HERO ────────────────────────────────────────────────────────── */
  .hero-band {
    background: #1a1f2e;
    color: #fff;
  }
  .hero-top {
    padding: 72px 28px 80px;
    max-width: 1200px;
    margin: 0 auto;
  }
  .hero-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    letter-spacing: 0.20em;
    text-transform: uppercase;
    color: var(--brand, #F32740);
    margin-bottom: 24px;
  }
  .hero-wordmark {
    font-family: 'Sora', sans-serif;
    font-size: clamp(72px, 10vw, 128px);
    font-weight: 900;
    letter-spacing: -0.03em;
    line-height: 0.95;
    color: #fff;
    margin: 0 0 28px;
    display: flex;
    align-items: baseline;
    flex-wrap: wrap;
    gap: 0;
  }
  .hero-wordmark :global(.brand-mark) {
    font-size: inherit;
    font-weight: inherit;
    letter-spacing: inherit;
    line-height: inherit;
  }
  .hero-wordmark :global(.aa) {
    color: var(--brand, #F32740);
    font-size: inherit;
  }
  .hero-desc {
    font-size: 17px;
    line-height: 1.55;
    color: rgba(255,255,255,0.70);
    max-width: 560px;
    margin-bottom: 32px;
  }
  @media (min-width: 1200px) { .hero-desc { white-space: nowrap; } }
  .hero-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }
  .hero-pill {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.80);
    border: 1px solid rgba(255,255,255,0.20);
    border-radius: 3px;
    padding: 5px 12px;
    background: rgba(255,255,255,0.04);
  }

  /* ── §3 STAT STRIP ──────────────────────────────────────────────────── */
  .stat-strip {
    background: var(--dark, #0c0d12);
    display: flex;
    align-items: stretch;
    border-bottom: 1px solid rgba(255,255,255,0.06);
  }
  .stat-strip-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 28px;
    display: flex;
    align-items: stretch;
    width: 100%;
  }
  .stat-col {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 36px 20px;
    gap: 6px;
  }
  .stat-divider {
    width: 1px;
    background: rgba(255,255,255,0.08);
    flex-shrink: 0;
    align-self: stretch;
    margin: 20px 0;
  }
  .stat-num {
    font-family: 'Sora', sans-serif;
    font-size: clamp(32px, 4vw, 48px);
    font-weight: 800;
    color: #fff;
    line-height: 1;
    letter-spacing: -0.02em;
  }
  .stat-red { color: var(--brand, #F32740); }
  .stat-lbl {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.45);
    text-align: center;
  }

  /* ── SHARED SECTION TOKENS ──────────────────────────────────────────── */
  .page-section { padding: 80px 0; }
  .section-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 28px;
  }
  /* Fix 3 (v2.5.3): wide container matching luxo */
  .section-inner--wide {
    max-width: 1400px;
  }
  .sec-eyebrow {
    display: flex;
    align-items: center;
    gap: 12px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--brand, #F32740);
    margin-bottom: 20px;
  }
  .sec-eyebrow::before {
    content: '';
    display: inline-block;
    width: 84px;
    height: 1.5px;
    background: var(--brand, #F32740);
    flex-shrink: 0;
  }
  .sec-title {
    font-family: 'Sora', sans-serif;
    font-size: clamp(24px, 3vw, 36px);
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: -0.01em;
    margin: 0 0 16px;
  }
  .sec-sub {
    font-size: 15px;
    line-height: 1.6;
    margin: 0 0 36px;
  }

  /* ── §4 BROWSE — Fix 4 (v2.5.3): white bg + light tiles matching luxo ─ */
  /* Before: dark2 bg, dark tiles, dark text/icons */
  /* After: white/off-white bg, light grey tiles, dark text + neutral grey icons */
  .sec-browse {
    background: var(--bg2, #f5f5f3);
  }
  .sec-browse .sec-eyebrow { color: var(--brand, #F32740); }
  .sec-browse .sec-eyebrow::before { background: var(--brand, #F32740); }
  /* Fix 4: sec-title and sec-sub removed (no headline in browse section) */
  .app-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
  }
  /* Fix 4 (v2.5.3): Light tile style matching luxo */
  .app-tile {
    background: #f8f8f6;
    border: 1px solid rgba(0,0,0,0.08);
    border-radius: 8px;
    padding: 28px 20px 24px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
    cursor: pointer;
    transition: box-shadow var(--transition), transform var(--transition), border-color var(--transition);
    text-align: center;
    color: inherit;
  }
  .app-tile:hover {
    box-shadow: 0 6px 24px rgba(0,0,0,0.10);
    transform: translateY(-2px);
    border-color: var(--brand, #F32740);
  }
  .app-tile[aria-pressed="true"] {
    border-color: var(--brand, #F32740);
    background: #fff0f2;
    box-shadow: 0 0 0 2px var(--brand, #F32740);
  }
  .app-tile[aria-pressed="true"] svg { stroke: var(--brand, #F32740); }
  .app-tile[aria-pressed="true"] .app-label { color: var(--brand, #F32740); }
  /* Fix 4: Dark neutral grey icons on light bg */
  .app-tile svg {
    width: 32px;
    height: 32px;
    stroke: #8a8f99;
    transition: stroke var(--transition);
  }
  .app-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--ink, #111418);
    font-weight: 700;
    line-height: 1.2;
  }
  .app-count {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    color: var(--ink3, #8a8f99);
  }

  /* ── §5 RED BANNER ──────────────────────────────────────────────────── */
  .red-banner {
    background: var(--brand, #F32740);
  }
  .red-banner-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 28px;
    display: flex;
    align-items: stretch;
  }
  .rb-col {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 36px 20px;
    gap: 6px;
  }
  .rb-div {
    width: 1px;
    background: rgba(255,255,255,0.25);
    flex-shrink: 0;
    align-self: stretch;
    margin: 20px 0;
  }
  .rb-num {
    font-family: 'Sora', sans-serif;
    font-size: clamp(28px, 3.5vw, 42px);
    font-weight: 800;
    color: #fff;
    line-height: 1;
    letter-spacing: -0.02em;
  }
  .rb-lbl {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.75);
    text-align: center;
  }

  /* ── §6 FEATURED FAMILIES — black bg canonical ──────────────────────── */
  .sec-featured { background: var(--dark, #0c0d12); }
  .sec-featured .sec-eyebrow { color: var(--brand, #F32740); }
  .sec-featured .sec-eyebrow::before { background: var(--brand, #F32740); }
  .sec-featured .sec-title { color: #fff; }
  .sec-featured .sec-sub { color: rgba(255,255,255,0.55); }
  .featured-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 16px;
  }
  .feat-card {
    background: var(--dark2, #14161e);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 6px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    transition: box-shadow var(--transition), transform var(--transition);
  }
  .feat-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0,0,0,0.35);
  }
  /* Fix 8 (v2.5.3): feat-card-top flex row — sub-tag left, tier-badge right, always visible */
  .feat-card-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    padding: 14px 14px 0;
    gap: 8px;
  }
  .feat-sub-tag {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.45);
    line-height: 1.4;
    flex: 1;
  }
  /* Fix 8 (v2.5.3): tier-badge always visible — no opacity:0 / hover-conditional */
  .tier-badge {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.10em;
    text-transform: uppercase;
    color: #fff;
    padding: 3px 7px;
    border-radius: 2px;
    white-space: nowrap;
    flex-shrink: 0;
    /* Static visibility — no opacity:0 here */
  }
  .tier-badge.eco     { background: rgba(255,255,255,0.10); color: #fff; border: none; }
  .tier-badge.pro     { background: #fff; color: #F32740; border: 1.5px solid #F32740; }
  .tier-badge.pro-plus { background: #F32740; color: #fff; border: none; }
  .fam-img-placeholder {
    width: 100%;
    background: rgba(255,255,255,0.04);
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .feat-placeholder {
    height: 160px;
  }
  .feat-card-body { padding: 14px 16px 18px; display: flex; flex-direction: column; gap: 6px; flex: 1; }
  .feat-name {
    font-family: 'Sora', sans-serif;
    font-size: 16px;
    font-weight: 700;
    color: #fff;
    margin: 0;
  }
  .feat-meta {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
  }
  .feat-maxw, .feat-skus {
    font-size: 12px;
    color: rgba(255,255,255,0.45);
  }
  .dlc-pill {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.08em;
    background: rgba(243,39,64,0.15);
    color: var(--brand, #F32740);
    padding: 2px 6px;
    border-radius: 3px;
  }
  .feat-cta {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.10em;
    color: var(--brand, #F32740);
    text-decoration: none;
    margin-top: auto;
  }
  .feat-cta:hover { text-decoration: underline; }

  /* ── §7 ALL FAMILIES — Fix 3 (v2.5.3): white outer bg matching luxo ─── */
  /* Before: dark2 bg, no eyebrow/headline, no filter-header */
  /* After: white/off-white bg, eyebrow + headline, filter-rail has FILTER BY header */
  .sec-all { background: var(--bg2, #f5f5f3); }
  .sec-title-dark { color: var(--ink, #111418); }
  .families-layout {
    display: flex;
    gap: 32px;
    align-items: flex-start;
  }
  /* Fix 3 (v2.5.3): filter-rail updated to white bg matching luxo */
  .filter-rail {
    width: 240px;
    flex-shrink: 0;
    background: #fff;
    border: 1px solid rgba(0,0,0,0.08);
    border-radius: 6px;
    padding: 20px;
    position: sticky;
    top: 140px;
    max-height: calc(100vh - 160px);
    overflow-y: auto;
  }
  /* Fix 3 (v2.5.3): filter-header matching luxo canonical */
  .filter-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
    padding-bottom: 12px;
    border-bottom: 1px solid rgba(0,0,0,0.08);
  }
  .filter-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--ink, #111418);
    font-weight: 700;
  }
  .filter-reset-btn {
    font-size: 11px;
    color: var(--brand, #F32740);
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
    text-decoration: none;
  }
  .filter-reset-btn:hover { text-decoration: underline; }
  .filter-group { margin-bottom: 16px; padding-bottom: 16px; border-bottom: 1px solid rgba(0,0,0,0.06); }
  .filter-group:last-of-type { border-bottom: none; margin-bottom: 0; }
  .filter-group-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--ink2, #3a3f47);
    margin: 0 0 10px;
  }
  .filter-opt {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 12px;
    color: var(--ink2, #3a3f47);
    margin-bottom: 5px;
    cursor: pointer;
  }
  .filter-opt input[type="checkbox"] { accent-color: var(--brand, #F32740); cursor: pointer; }
  .filter-opt input[disabled] { opacity: 0.35; cursor: not-allowed; }
  .filter-count { color: var(--ink3, #8a8f99); font-size: 11px; margin-left: auto; }
  .filter-range-wrap { padding: 4px 0; }
  .filter-range { width: 100%; accent-color: var(--brand, #F32740); opacity: 0.35; cursor: not-allowed; }
  .filter-range-labels { display: flex; justify-content: space-between; font-size: 10px; color: var(--ink3, #8a8f99); margin-top: 4px; }
  .af-grid-col { flex: 1; min-width: 0; }
  .grid-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 20px;
    padding-bottom: 12px;
    border-bottom: 1px solid rgba(0,0,0,0.08);
  }
  .grid-count {
    font-size: 13px;
    color: var(--ink2, #3a3f47);
  }
  .sort-wrap select {
    background: #fff;
    border: 1px solid rgba(0,0,0,0.12);
    color: var(--ink2, #3a3f47);
    font-size: 12px;
    padding: 5px 10px;
    border-radius: 3px;
    cursor: not-allowed;
  }
  .all-fam-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 14px;
  }
  /* Fix 3 (v2.5.3): af-card dark on white bg — kept dark for contrast */
  .af-card {
    background: #0d111a;
    border: 1px solid rgba(255,255,255,0.06);
    border-radius: 6px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    transition: transform var(--transition), box-shadow var(--transition);
  }
  .af-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0,0,0,0.35);
  }
  .af-card.hidden { display: none; }
  /* Fix 8 (v2.5.3): af-card-top flex row — always visible tier badge */
  .fam-sub-tag {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.45);
    line-height: 1.4;
    flex: 1;
  }
  .af-card-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    padding: 10px 10px 0;
    gap: 6px;
  }
  .fam-img-placeholder.af-placeholder {
    height: 140px;
    margin: 10px;
    border-radius: 4px;
    background: rgba(255,255,255,0.04);
  }
  .af-card-bottom {
    padding: 8px 12px 14px;
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    margin-top: auto;
  }
  .af-sub {
    display: block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    letter-spacing: 0.10em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.35);
    margin-bottom: 3px;
  }
  .af-name {
    font-family: 'Sora', sans-serif;
    font-size: 14px;
    font-weight: 700;
    color: #fff;
    margin: 0 0 6px;
  }
  .af-meta {
    display: flex;
    align-items: center;
    gap: 6px;
    flex-wrap: wrap;
  }
  .af-maxw, .af-skus {
    font-size: 11px;
    color: rgba(255,255,255,0.40);
  }
  .af-dlc { font-size: 9px; }
  .af-cta {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.08em;
    color: var(--brand, #F32740);
    text-decoration: none;
    flex-shrink: 0;
  }
  .af-cta:hover { text-decoration: underline; }
  .af-no-results { padding: 40px; text-align: center; color: var(--ink3, #8a8f99); font-size: 14px; }
  .reset-inline { background: none; border: none; color: var(--brand, #F32740); cursor: pointer; font-size: 14px; text-decoration: underline; }

  /* ── §8 LEGACY ──────────────────────────────────────────────────────── */
  .sec-legacy { background: var(--bg2, #f5f5f3); }
  .sec-legacy .sec-eyebrow { color: var(--brand, #F32740); }
  .sec-legacy .sec-eyebrow::before { background: var(--brand, #F32740); }
  .sec-legacy .sec-title { color: var(--ink, #111418); }
  .legacy-box {
    border: 1px solid rgba(0,0,0,0.10);
    border-radius: 8px;
    background: #fafaf8;
    padding: 36px 40px;
    max-width: 800px;
  }
  .legacy-h3 {
    font-family: 'Sora', sans-serif;
    font-size: 20px;
    font-weight: 700;
    color: var(--ink, #111418);
    margin-bottom: 12px;
  }
  .legacy-body {
    font-size: 15px;
    line-height: 1.65;
    color: var(--ink2, #3a3f47);
    margin-bottom: 20px;
  }
  .legacy-cta {
    font-size: 14px;
    font-weight: 600;
    color: var(--brand, #F32740);
    text-decoration: none;
  }
  .legacy-cta:hover { text-decoration: underline; }

  /* ── §9 GET STARTED ─────────────────────────────────────────────────── */
  .sec-get-started { background: var(--bg3, #eceae4); }
  .sec-get-started .sec-eyebrow { color: var(--brand, #F32740); }
  .sec-get-started .sec-eyebrow::before { background: var(--brand, #F32740); }
  .sec-get-started .sec-title { color: var(--ink, #111418); }
  .sec-get-started .sec-sub { color: var(--ink2, #3a3f47); }
  .cta-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
  }
  .cta-card {
    background: #fff;
    border: 1px solid rgba(0,0,0,0.08);
    border-radius: 6px;
    padding: 28px 24px;
    display: flex;
    flex-direction: column;
    gap: 12px;
    transition: box-shadow var(--transition), transform var(--transition);
  }
  .cta-card:hover {
    box-shadow: 0 6px 24px rgba(0,0,0,0.08);
    transform: translateY(-2px);
  }
  .cta-title {
    font-family: 'Sora', sans-serif;
    font-size: 18px;
    font-weight: 700;
    color: var(--ink, #111418);
    margin: 0;
  }
  .cta-body {
    font-size: 14px;
    line-height: 1.55;
    color: var(--ink2, #3a3f47);
    flex: 1;
    margin: 0;
  }
  .cta-link {
    font-size: 13px;
    font-weight: 700;
    color: var(--brand, #F32740);
    text-decoration: none;
    letter-spacing: 0.02em;
  }
  .cta-link:hover { text-decoration: underline; }

  /* ── §10 RELATED COLLECTIONS ────────────────────────────────────────── */
  .sec-related { background: var(--bg2, #f5f5f3); }
  .sec-related .sec-eyebrow { color: var(--brand, #F32740); }
  .sec-related .sec-eyebrow::before { background: var(--brand, #F32740); }
  .sec-related .sec-title { color: var(--ink, #111418); }
  .related-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
  }
  .related-card {
    border: 1px solid rgba(0,0,0,0.08);
    border-radius: 6px;
    padding: 28px 24px;
    display: flex;
    flex-direction: column;
    gap: 10px;
    transition: box-shadow var(--transition), transform var(--transition);
  }
  .related-card:hover {
    box-shadow: 0 6px 24px rgba(0,0,0,0.08);
    transform: translateY(-2px);
  }
  /* Fix 11 (v2.5.3): Coming soon card treatment */
  .related-card--disabled {
    opacity: 0.85;
    cursor: default;
  }
  .related-card--disabled:hover {
    box-shadow: none;
    transform: none;
  }
  .related-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--ink3, #8a8f99);
  }
  .related-name {
    font-family: 'Sora', sans-serif;
    font-size: 22px;
    font-weight: 800;
    color: var(--ink, #111418);
    letter-spacing: -0.01em;
  }
  .related-desc {
    font-size: 13px;
    line-height: 1.55;
    color: var(--ink2, #3a3f47);
    flex: 1;
    margin: 0;
  }
  .related-link {
    font-size: 13px;
    font-weight: 700;
    color: var(--brand, #F32740);
    text-decoration: none;
  }
  .related-link:hover { text-decoration: underline; }
  /* Fix 11 (v2.5.3): Disabled "Coming soon" CTA */
  .related-link-disabled {
    font-size: 13px;
    font-weight: 600;
    color: var(--ink3, #8a8f99);
    cursor: default;
  }

  /* ── RESPONSIVE ─────────────────────────────────────────────────────── */
  @media (max-width: 1399px) {
    .all-fam-grid { grid-template-columns: repeat(3, 1fr); }
  }
  @media (max-width: 1199px) {
    .featured-grid { grid-template-columns: repeat(3, 1fr); }
    .app-grid { grid-template-columns: repeat(4, 1fr); }
    .cta-grid { grid-template-columns: repeat(2, 1fr); }
    .related-grid { grid-template-columns: repeat(2, 1fr); }
    .stat-strip { flex-wrap: wrap; }
    .stat-col { min-width: 50%; }
  }
  @media (max-width: 1023px) {
    .families-layout { flex-direction: column; }
    .filter-rail { width: 100%; position: static; max-height: none; }
    .all-fam-grid { grid-template-columns: repeat(2, 1fr); }
    .featured-grid { grid-template-columns: repeat(2, 1fr); }
    .rb-col { padding: 24px 12px; }
    .rb-num { font-size: 32px; }
  }
  @media (max-width: 767px) {
    .hero-wordmark { font-size: clamp(52px, 14vw, 72px); }
    .app-grid { grid-template-columns: repeat(2, 1fr); }
    .featured-grid { grid-template-columns: 1fr; }
    .all-fam-grid { grid-template-columns: 1fr; }
    .cta-grid { grid-template-columns: 1fr; }
    .related-grid { grid-template-columns: 1fr; }
    .red-banner-inner { flex-wrap: wrap; }
    .rb-col { min-width: 33%; }
    .rb-div { display: none; }
    .legacy-box { padding: 24px 20px; }
  }
  /* Hidden family-cards */
  .af-card.hidden { display: none !important; }
</style>

<script>
  /* Interactive sub-category filter (live).
   * Checkbox name=filter-subcategory, value=kebab-case token.
   * Each .af-card has data-subcategory with a single token.
   * OR logic: card visible if any checked token matches its token. */
  (function () {
    var grid = document.getElementById('all-fam-grid');
    var countEl = document.getElementById('grid-count');
    var noResults = document.getElementById('no-results');
    var resetBtn = document.getElementById('reset-filter');
    var resetInline = document.getElementById('reset-inline');
    var cbs = Array.from(document.querySelectorAll('.app-filter-cb'));
    var appTiles = Array.from(document.querySelectorAll('.app-tile'));

    function getActiveTokens() {
      return cbs.filter(function (cb) { return (cb as HTMLInputElement).checked; })
        .map(function (cb) { return (cb as HTMLInputElement).value; });
    }

    function applyFilter() {
      var tokens = getActiveTokens();
      var cards = grid ? Array.from(grid.querySelectorAll('.af-card')) : [];
      var visible = 0;
      cards.forEach(function (card) {
        var cardApp = (card as HTMLElement).dataset.subcategory || '';
        var show = tokens.length === 0 || tokens.indexOf(cardApp) !== -1;
        (card as HTMLElement).classList.toggle('hidden', !show);
        if (show) visible++;
      });
      if (countEl) {
        /* Fix 1 (v2.5.3): 17→15 */
        countEl.textContent = tokens.length === 0
          ? 'Showing 15 families'
          : 'Showing ' + visible + ' famil' + (visible === 1 ? 'y' : 'ies');
      }
      if (noResults) noResults.style.display = visible === 0 ? 'block' : 'none';
      /* sync tile pressed state */
      appTiles.forEach(function (tile) {
        var tok = (tile as HTMLElement).dataset.app || '';
        (tile as HTMLElement).setAttribute('aria-pressed', tokens.indexOf(tok) !== -1 ? 'true' : 'false');
      });
    }

    cbs.forEach(function (cb) { cb.addEventListener('change', applyFilter); });

    appTiles.forEach(function (tile) {
      tile.addEventListener('click', function () {
        var tok = (tile as HTMLElement).dataset.app || '';
        var cb = cbs.find(function (c) { return (c as HTMLInputElement).value === tok; }) as HTMLInputElement | undefined;
        if (cb) {
          cb.checked = !cb.checked;
          applyFilter();
          var sec = document.getElementById('sec-all-families');
          if (sec) sec.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
      });
    });

    function resetAll() {
      cbs.forEach(function (cb) { (cb as HTMLInputElement).checked = false; });
      applyFilter();
    }
    if (resetBtn) resetBtn.addEventListener('click', resetAll);
    if (resetInline) resetInline.addEventListener('click', resetAll);
  })();
</script>
